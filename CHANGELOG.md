# Changelog — Internal Development History

This file documents all public and internal development iterations of DashCast.
See [README.md](README.md) for the project overview and installation instructions.

---

## Stable releases

| Version | versionCode | Summary |
|---------|-------------|---------|
| **1.0.1** | 155 | **Bug fix — cluster screen-size selector in Settings was not clickable.** In the Settings screen, the three cluster-type options (`8.8"`, `12.3"`, `10.25"`) lived inside a `RadioGroup` (`rg_cluster_type`), but the `MaterialRadioButton` widgets had `android:clickable="false"` and `android:focusable="false"`, and the surrounding `LinearLayout` rows had **no id, no click handler, and no selectable background**. Because the radio buttons are nested inside another `LinearLayout` (not direct children of the `RadioGroup`), `RadioGroup`'s built-in auto-check mechanism never received any tap event either — so the entire selection was inert: the user could not change the saved size. Fix: each of the three rows now has its own id (`row_cluster_88`, `row_cluster_123`, `row_cluster_1025`), `android:clickable="true"`, `android:focusable="true"` and `android:background="?attr/selectableItemBackground"` (Material ripple). In `SettingsActivity.wireListeners()`, each row delegates its click to `rgClusterType.check(R.id.rb_XX)`, which in turn triggers the existing `OnCheckedChangeListener` that persists the corresponding sendInfo command (29 = 8.8" / 30 = 12.3" Seal EU / 31 = 10.25") into `cluster_screen_size_cmd` in SharedPreferences. The downstream wiring is preserved end-to-end: long-pressing the **Stop projection** button in `MainActivity` calls `originCluster()`, which reads `getClusterTypeCmd()` and forwards it to `AdbLocalClient.restoreOriginCluster(ctx, screenSizeCmd, pkg, cb)`, so the user-selected cluster size is correctly applied when restoring the native BYD instrument cluster. No business logic was modified — only XML attributes on the three rows and three click handlers in `SettingsActivity`. |
| **1.0.0** | 154 | **Official stable release** — exit from the alpha/beta channel after extensive in-car validation on BYD Seal EU (DiLink 3.0). Consolidates the entire 0.9.x M3 redesign, Settings/Welcome i18n pass, OTA flow, long-press "Restore Origin Cluster" bottom-sheet, audit lint/javac cleanup (lint 169→71, javac warnings 49→0) and the 12-locale documentation rebuild. |

## Pre-releases

| Version | versionCode | Summary |
|---------|-------------|---------|
| **1.2.0-build185** | 185 | **New DiLink 2 recon diagnostic tab — reflection-only test suite (L1–L15) for the alps / k65v1_64_bsp / MT6765 / Android 9 platform.** Two DL2 field reports (`byd_log_20260521_220933.log` and `byd_report_20260521_222022.log`, 327 lines of SysInfo dump) confirmed the DL2 environment differs from DL3/DL5 on every dimension that matters for projection: **(a)** brand=`alps`, manufacturer=`alps`, product=`full_k65v1_64_bsp`, hardware=`mt6765`, API=28, `ro.byd.*` properties ALL undefined — no BYD identity exposed via system properties; **(b)** ADB-over-TCP `127.0.0.1:5555` is CLOSED (`ECONNREFUSED` confirmed twice in the log, both at boot for `SYSTEM_ALERT_WINDOW` auto-grant and on the `executeShellWithResult "echo ok"` smoke test) — our entire `AdbLocalClient` shell stack is unreachable on DL2; **(c)** `DisplayManager` exposes a single display (Built-in Screen 1280×660 @160dpi, no PRESENTATION-typed display) even though the vehicle physically has a digital cluster screen — the cluster output is not surfaced to the Android display framework; **(d)** `android.app.ActivityTaskManager` is absent (IATM was introduced in API 29, this device is API 28) — every current reflection path that targets IATM throws ClassNotFoundException; **(e)** no XDJA / Auto_container / containerservice / clusterdebug / crosscontrol / windowmanagement / smarttravel / magicwindow package is installed — the BYD ecosystem we built DL3/DL5 support on top of does not exist on this platform; only `com.byd.cluster v1.0` and `com.byd.appstartmanagement v1.0` are present (vs v1.5.0.0 of appstartmanagement on DL5). **Positive surface confirmed in the log:** `SurfaceControl` accessible after `unlockHiddenApis`, `ActivityOptions.setLaunchDisplayId` available, 3/5 BYDAuto*Device SDK classes loadable via reflection (Speed / Energy / Gearbox). **Design choice for build 185.** Because the shell stack is dead on DL2, the new test runner is built on a completely different foundation than DL5: **zero shell calls**, every probe uses Java APIs callable from the app process — `SystemProperties.get` via reflection, `ServiceManager.listServices/getService` via reflection, `PackageManager` queries, `DisplayManager` + reflective `DisplayManagerGlobal.getDisplayIds`, `IActivityManager` method enumeration via reflection, raw `Socket` TCP probes on localhost ADB ports, and `/proc` + `/sys` scans via `java.io.File`. This means the suite runs and produces useful data even when ADB-TCP is unreachable. **(1) `dilink2/DiLink2TestRunner.java` (new file, 15 tests, ~580 LoC).** Self-contained: own `Status` enum, own `TestDef` / `TestResult` / `Listener` types, single-thread executor named `dilink2-test-runner`, ordered catalog, dispatcher. **L1 — Platform fingerprint.** Dumps the full `Build.*` triplet (BRAND / MANUFACTURER / MODEL / PRODUCT / DEVICE / HARDWARE / BOARD / DISPLAY / FINGERPRINT / TAGS / SDK_INT / RELEASE) plus 26 selected `ro.*`, `persist.sys.*`, `sys.*` and `service.adb.tcp.port` properties read via reflected `SystemProperties.get`. PASS when `Build.BRAND=alps` AND `Build.PRODUCT` contains `k65v1` (DL2 signature), WARN otherwise (so the runner is informative on DL3/DL5 too without breaking them). **L2 — ADB local TCP ports probe.** Pure-Java `Socket.connect(InetSocketAddress, 200ms)` on `127.0.0.1` ports 5037 / 5554 / 5555 / 5556 / 4444, with timeout-bucketing of error classification (OPEN vs CLOSED vs TIMEOUT vs ERR). WARN when 0 ports open (expected on DL2 — confirms the shell-disabled environment), PASS when any port responds. **L3 — Multi-display reflective scan.** `DisplayManager.getDisplays()` + `DisplayManager.getDisplays(DISPLAY_CATEGORY_PRESENTATION)` cross-checked against reflective `DisplayManagerGlobal.getDisplayIds()` — the latter returns the raw list before category filtering and sometimes surfaces ids hidden from the standard enumeration. **L4 — SurfaceFlinger physical displays via IBinder.** `ServiceManager.getService("SurfaceFlinger")` + `IBinder.getInterfaceDescriptor()` — deliberately does NOT call `transact()` with custom codes because the SurfaceFlinger transaction layout has been reshuffled across AOSP forks and a wrong code on a vendor binder can SIGSEGV the calling process. PASS on binder reachability alone. **L5 — DRM / framebuffer inventory.** Lists `/sys/class/drm`, `/dev/graphics`, `/sys/class/graphics` via `java.io.File.listFiles()`; PASS when fb1/fb2 OR multiple `card0-*` connectors are visible (= a second hardware display exists outside Android's view), WARN on single framebuffer. **L6 — ServiceManager full inventory + filter.** Reflective `ServiceManager.listServices()` (returns a `String[]` of every registered binder service) then regex-filtered on `cluster|display|secondary|byd|alps|auto|mirror|magic|window|fission|xdja|projection|cross`. Each match prefixed with `  ` for the report. **L7 — Cluster service candidates brute probe.** `ServiceManager.getService()` on 33 hand-curated candidate names spanning DL3/DL5 conventions (`cluster`, `byd_cluster`, `Auto_container`, `auto_container`), MTK conventions (`mtk_cluster`, `mtk_displayfeature`), alps conventions (`alps_cluster`, `alps_display`) and generic suspects (`secondary_display`, `displayfeature`, `mirror`, `magicwindow`, `crosscontrol`, `xdja_container`, `byd_carapi`, `BYDMgmt`, `byd_datacached`, `IBYDCDRService`, `projection`, `carprojection`, `fission`, `appstartmanagement`, etc.); for each PRESENT binder also dumps `IBinder.getInterfaceDescriptor()` so we know the actual AIDL interface name. **L8 — IActivityManager method enumeration.** Reflection on `android.app.IActivityManager` (NOT `IActivityTaskManager` since DL2 is API 28) filtered on `movetask|setlaunch|startactivity|moveactivitytask|settaskwindowing|getfocusedstack|attachapplication` — each entry shows the full method signature so we can build the right typed invocation path. **L9 — ActivityOptions.setLaunchDisplayId smoke.** Builds `ActivityOptions.makeBasic()`, invokes `setLaunchDisplayId(0)` reflectively, then `toBundle()` to confirm the bundled keys are populated (PASS expected, the SysInfo dump already showed this method is present on DL2). **L10 — BYD packages dynamic scan.** `PackageManager.getInstalledPackages(0)` filtered on `com.byd.*` / `com.alps.*` / `com.xdja.*` / `*cluster*` / `*dilink*` — captures package name + versionName + versionCode + APK path with zero shell. **L11/L12 — Manifest deep dive on `com.byd.cluster` and `com.byd.appstartmanagement`** (the only two BYD packages of interest installed on DL2): `getPackageInfo(GET_ACTIVITIES|GET_SERVICES|GET_RECEIVERS|GET_PROVIDERS|GET_PERMISSIONS)` produces a structured dump of every declared component with exported flag, all declared permissions with protection level, and all requested permissions — lets us see exactly what these packages can do and what entry points they expose. SKIPPED gracefully when the package is not installed. **L13 — BYD SDK classpath probe.** `Class.forName` + reflective `getInstance()` on 8 `BYDAuto*Device` classes (Speed / Energy / Gearbox / AC / AirConditioner / Door / Light / Wiper). Distinguishes "class loadable" from "instance obtainable" — the SysInfo dump showed 3/5 of the original 5 we probed work on DL2; the new probe widens that to 8 classes for a fuller picture. **L14 — `/proc` cluster process scan.** Pure-Java scan of `/proc/<pid>/cmdline` (`new File("/proc").listFiles()`) filtered on `cluster|fission|projection|secondary|surface|display`, capped at 40 matches — reveals which native daemons (e.g. `surfaceflinger`, MTK display daemons, vendor cluster handlers) are visible to the app uid even when the binder facing them isn't. **L15 — Hidden-API reachability sanity.** Reflective `ServiceManager.getService` + `getInterfaceDescriptor()` on 6 core system binders (`window`, `activity`, `package`, `display`, `input`, `power`); PASS when at least 4 are reachable — confirms the app process can still talk to system services despite SELinux on API 28 (relevant because some recon strategies on DL2 will need a typed binder path instead of shell). All exceptions inside the test bodies are caught and reported as `FAIL` with `<ExceptionClass>: <message>`; the dispatcher's `Throwable` catch is a defense-in-depth so a single test crash never breaks the suite. **(2) `res/layout/include_diag_dilink2.xml` (new file).** Mirrors the DL5 panel design but simpler — no D8-style live launch, no app picker, just: a header card with title `Plateforme DiLink 2 — Recon`, dynamic subtitle (`Produit : %1$s · brand : %2$s · API %3$d`), a signature pill (`DL2 détectée` / `non-DL2`), the run/copy counters, two Material buttons `Lancer tous` / `Envoyer (.log)`, and a `LinearLayout ll_dl2_test_list` populated programmatically from `DiLink2TestRunner.catalog()` using the existing `item_beta_test.xml` row layout (full re-use to avoid layout duplication). **(3) `DiagActivity.java` wiring.** New tab constant `TAB_DILINK2 = 7` (Sniffer shifted to 8). New `panelDl2` field bound in `bindTabs()`, `showPanelForTab` now toggles a 5th panel. New `bindDl2Panel()` resolves DL2 signature from `Build.BRAND` + `Build.PRODUCT` at bind time (no Platform changes — we deliberately keep `Platform.java` untouched, the DL2 signature detection is a UI-side hint only). New `prepareDl2TestRows()` / `bindDl2Row()` / `runDl2AllTests()` / `updateDl2Counters()` / `copyDl2Report()` mirror exactly the DL5 lifecycle, with their own `dl2RowViews` / `dl2LastResults` lists so no DL5 / Beta state can collide. The copy-report path goes through `AppLogger.shareWithReport()` to ship as `.log` attachment. **(4) `activity_diag.xml` — new `<TabItem android:text="@string/diag_tab_dilink2"/>` inserted between DiLink 5 and Sniffer + new `<include android:id="@+id/panel_dilink2" layout="@layout/include_diag_dilink2"/>` inside `fl_diag_content`. **(5) `strings.xml` — 9 new keys** (`diag_tab_dilink2`, `diag_dl2_header_title`, `diag_dl2_header_subtitle`, `diag_dl2_header_subtitle_fmt`, `diag_dl2_pill_unknown`, `diag_dl2_pill_detected`, `diag_dl2_pill_other`, `diag_dl2_counters_fmt`). French-only for now (the 12 locale files are intentionally NOT updated yet — will batch with the next translation pass once the DL2 panel labels stabilize after field testing). **No production-path changes.** Zero touches to `ClusterManager`, `AdbLocalClient`, `Platform.java`, `BetaTestRunner`, `DiLink5TestRunner`, no new permissions, no new dependencies, no new protocol verbs. DL3 and DL5 behaviour is bit-for-bit identical to build 184. **Build.** `versionCode` 184 → 185, `versionName` pinned `"1.2.0"` per channel policy. **(6) Horizontal swipe navigation in Diag (Option B, lightweight).** With 9 tabs (Cluster / Display / ADB local / Système / ADAS / Beta Engine / DiLink 5 / DiLink 2 / Sniffer) the tab bar is getting long. Build 185 attaches a `GestureDetector.SimpleOnGestureListener` to the `FrameLayout fl_diag_content` (single touch listener, ~40 LoC, no architecture change): a clear horizontal fling (|dx| ≥ 120 px AND |dx| ≥ 1.5×|dy| AND |vx| ≥ 220 px/s) selects the next or previous tab via `tabs.getTabAt(next).select()` which reuses the existing `OnTabSelectedListener` so the same `showPanelForTab` path runs. The listener returns `false` so child scrollers (NestedScrollView in DL5/DL2/Sniffer panels, RecyclerView rows in test lists, vertical scroll inside SysInfo report) keep working untouched — only clear horizontal flings are intercepted. The threshold checks reject diagonal swipes and tap noise. No animation between panels (the bascule is dry, like the current tab click) — a proper `ViewPager2 + FragmentStateAdapter` refactor with animated slide is queued for a later build (see `/memories/repo/byd-project.md` TODO: needs 5 new Fragment classes + refactor of all 5 `bind*Panel` methods + retest on DL3/DL5/DL2). Wiring: new `attachSwipeNavigation(View)` method called once in `bindTabs()`. **Validation plan.** (a) Install on the DL2 testeur device, open Diag, navigate to the new DiLink 2 tab, confirm the signature pill reads `Signature : DL2 détectée`, press `Lancer tous`, wait for the 15-test pass (~3 s total — all tests are sub-second since none of them hit shell). (b) Press `Envoyer (.log)`, share the resulting `byd_report_*.log` via Telegram. (c) Cross-check on a DL3 or DL5 device that the tab is still visible (we did NOT gate it behind Platform.isDiLink2 since we have no DL2 detection in Platform yet) and that pressing `Lancer tous` produces a report where L1 reports WARN ("not a DL2 signature") and the other tests still produce useful data — confirms the suite is non-destructive on non-DL2 platforms. The DL2 report will tell us, for the first time, (i) whether any non-DisplayManager surface exposes a second physical display via SurfaceFlinger or DRM, (ii) which binder services BYD or alps registered for cluster control, (iii) what the `com.byd.cluster` package on DL2 actually contains and exposes, (iv) which `IActivityManager` methods are available for typed display routing on API 28 without IATM, (v) whether the ADB-TCP situation can be unblocked from the device side. Based on these answers, build 186 will design the actual DL2 cluster projection strategy. |
| **1.2.0-build184** | 184 | **Diag UI — new ADAS quick-toggle tab + DiLink 5 test polish based on the build-183 field log.** First DL5 field log (`byd_report_20260522_092905.log`, build 183) gave us actionable signal on three fronts: (a) clusterdebug v1.6.1.4 enumeration confirmed AutoContainer `sendInfo` codes 12 (显示Adas / Show ADAS) and 13 (关闭Adas / Hide ADAS) are universal across all DiLink platforms (DL3 / Di4 / DL5 / DL6) — stripped only on R-series; (b) D28 was running a `sendInfo(30)` screen-size hint that is a no-op on DL5 because the cluster never reads it (the corresponding slot is empty in clusterdebug `_Di5` variant); (c) D10 was re-`cat`ing every whitelisted APK on every run even though the destination filename already embeds `versionCode`. Build 184 turns those three findings into shipped fixes plus a new always-visible quick-toggle for the ADAS overlay. **(1) New `ADAS` diagnostic tab** replaces the previously unused `Stress test` placeholder (tab position #5). Layout `include_diag_adas.xml` (190 lines, 3 M3 cards in a `NestedScrollView`): (a) header card with title, subtitle, a runtime status pill showing the resolved service name (`Auto_container` on DL3/Di4 / `auto_container` on DL5+), and two filled / tonal `MaterialButton`s `Afficher ADAS (12)` and `Masquer ADAS (13)`; (b) result card with a monospace `tv_adas_result` text view showing the last issued command and the raw shell response (Parcel hex or error); (c) info card explaining the underlying `service call <svc> 2 i32 1000 i32 <12|13> s16 ""` invocation, the service-name dispatch rule, and that R-series is unsupported. **(2) Wiring in `DiagActivity.java`**: `TAB_STRESS` (=4) renamed to `TAB_ADAS`, new `panelAdas` field bound in `bindTabs()`, `showPanelForTab` gated on `isAdas`, `TAB_STRESS` removed from the coming-soon switch. New `bindAdasPanel()` resolves the service name once at bind time (visible in the pill) and wires the two buttons to `sendAdasCmd(12)` / `sendAdasCmd(13)`. `sendAdasCmd(int)` re-resolves the service name on every press (so toggling the DL5 override in Settings is picked up without restarting Diag), builds the exact `service call` command line, displays `$ <cmd>\n…envoi en cours…` on press, and writes back the raw daemon response via `safeRunOnUiThread`. Error path prefixes the response with `ERREUR:`. Service dispatch uses `Platform.get().isDiLink5(this)` (try/catch wraps reflection so a future Platform change cannot crash the panel — falls back to the DL3 default `Auto_container`). **(3) `activity_diag.xml`**: 5th `<TabItem>` now references `@string/diag_tab_adas` (new key, value `"ADAS"`, untranslated acronym, added to `values/strings.xml` next to the legacy `diag_tab_stress` which is left in place to keep the 12 locale files unchanged); new `<include android:id="@+id/panel_adas" layout="@layout/include_diag_adas"/>` inserted into `fl_diag_content`. **(4) D10 — APK extraction cache hit.** Filename already embeds `versionCode` (`<pkg>_v<versionCode>.apk`), so if the destination file exists with `length() > 0` we now skip the `cat` redirect entirely, increment a new `cached` counter, and log `↻ <pkg>  (cached, NN KB)`. Message summary reads `N APK(s) ready (X cached), Y skipped — adb pull the dir` instead of the build-183 `N APK(s) extracted`. On a re-run with no new packages, D10 now produces zero shell calls (was N calls per whitelisted package, each up to 15 s of `cat` budget). **(5) D23 rewritten end-to-end.** Build 183 D23 looped over binder transaction codes 0..8 with a neutral payload to enumerate the auto_container API surface — useful one-off but redundant after the clusterdebug enumeration (codes 0/1/2 are interface-transaction / ping / sendInfo respectively, the rest are not transactions but sendInfo *codes*). D23 now sends `service call auto_container 2 i32 1000 i32 2 s16 ""` (= sendInfo code 2 = 所有警告灯点亮 / `All warning lamps ON`), waits 3 s via `Thread.sleep(3000)`, then sends `service call auto_container 2 i32 1000 i32 3 s16 ""` (= sendInfo code 3 = 所有警告灯熄灭 / `All warning lamps OFF`). Status `WARN` with message asking the user to visually confirm that the warning lamps lit up for ~3 s and went off again; `FAIL` if `service auto_container does not exist`. This is a much stronger test than scanning the binder API: it cross-validates that (a) the snake-case `auto_container` service name reaches the cluster on DL5, (b) the sendInfo path is end-to-end functional and (c) the cluster Qt side honours the published codes from the clusterdebug enumeration. **(6) D28 — dropped the useless `sendInfo(30)` step.** clusterdebug `_Di5` variant has an empty slot at code 30 (was `设置屏幕尺寸` / `Set screen size` on DL3 / Di4, dropped on DL5). The screen-size hint is a no-op on this platform. New D28 flow: `logcat -c` → `sendInfo(16)` (Qt standby ON, real projection trigger) → sleep 3 s → `dumpsys display` → logcat filtered on `auto_container | AutoContainer | cluster | projection | fission | XDJA | magicwindow | MagicWindow | containerservice` (last 80 lines) → cleanup `sendInfo(18)` (close) + `sendInfo(0)` (refresh). Catalog description updated to match. The test now exercises only the real projection trigger, no padding. **(7) Documentation.** Added `doc_api/APK_com.byd.clusterdebug_CODES.md`: full enumeration of clusterdebug v1.6.1.4 (versionCode 10601004) sendInfo codes across all three `infoListInit*` variants (`generic` for DL3/Di4, `_Di5` for DL5, `_R` for R-series), with side-by-side zh / EN translations for every populated slot. Used as the canonical reference for both the ADAS panel codes (12/13) and the D23 warning-lamp cycle (2/3), and as the matrix to audit which AutoContainer codes are platform-stable vs platform-stripped. **(8) Behavioural contract.** Production hot path (`ClusterManager.sendActivationSequence` and the rest of the cluster lifecycle) is unchanged. The ADAS panel reuses the existing `AdbLocalClient.executeShellWithResult` background path — no new thread, no new permission, no new system Context. The panel is read-only / write-only with respect to projection state: pressing `Afficher ADAS` while a projection is running just overlays the warning lamps; pressing `Masquer ADAS` does not affect projection. **(9) Build.** `versionCode` 183 → 184, `versionName` pinned `"1.2.0"` per channel policy. Touched files outside CHANGELOG / gradle: `DiagActivity.java` (TAB rename + `panelAdas` field + `bindAdasPanel` + `sendAdasCmd` + `autoContainerSvcName` helper), `dilink5/DiLink5TestRunner.java` (D10 cache logic + D23 full rewrite + D28 sendInfo(30) drop + catalog text for D23 and D28), `res/layout/activity_diag.xml` (tab text + new include), `res/layout/include_diag_adas.xml` (new 190 lines), `res/values/strings.xml` (added `diag_tab_adas="ADAS"`). No new dependencies, drawables (reuses `ic_stethoscope`), permissions, or protocol verbs. **(10) Validation plan.** (a) Install on a DL3 device, open Diag → ADAS tab → confirm pill reads `Auto_container`, press `Afficher ADAS (12)` → expect warning lamps lit on cluster + Parcel response in the result card, press `Masquer ADAS (13)` → expect lamps off. (b) Install on a DL5 device, confirm pill reads `auto_container` (snake_case), repeat the cycle, then run Diag → DiLink 5 → D23 → confirm WARN status with the visual-cycle message, run D10 twice in a row → second pass shows all rows as `cached`, run D28 → confirm the report no longer contains a `sendInfo(30)` section. (c) Toggle the DL5 override in Settings on a DL3 device → reopen Diag → ADAS tab → confirm pill switches between `Auto_container` and `auto_container` and that subsequent button presses use the corresponding service name in the displayed `$` command. |
| **1.2.0-build183** | 183 | **DiLink 5 reconnaissance — 10 new tests (D21–D30) to map the projection mechanism.** First DL5 field log (build 182, `byd_report_20260522_092905.log`) revealed: (a) `auto_container` service IS reachable on DL5 (D11/D12 PASS via explicit snake_case) but our entire production shell path hardcodes `AutoContainer` (PascalCase) — fix DEFERRED pending APK reverse-engineering; (b) DL5 has 4 displays vs DL3's 3 — mystery display #2 is non-PRESENTATION-typed, holds a single window from PID 1326 / uid 10054 since boot; (c) `IATM.setTaskWindowingMode(int,int,boolean)` is absent on this build — BYD stripped or renamed it; (d) `com.byd.containerservice v12` holds `MANAGE_ACTIVITY_TASKS` + `REMOVE_TASKS` + an `AutoDisplayService` — it's the new gatekeeper; (e) 113 BYD packages inventoried, 7 APKs successfully extracted by D10 (clusterdebug, containerservice, appstartmanagement, smarttravel, crosscontrol, providers.appstartup, car.server) for offline jadx work. Build 183 adds the reconnaissance surface needed to answer: \"how does BYD's own code project on the cluster on DL5?\" before we commit to a fix. **D21 — All displays full inventory.** `DisplayManager.getDisplays()` (no PRESENTATION filter) + reflective `Display.getDisplayInfo(DisplayInfo)` for every id: `type`, `ownerUid`, `ownerPackageName`, `logicalWidth/Height`, `logicalDensityDpi`, `removeMode`, `uniqueId` + full flag-bit breakdown (PROTECTED / SECURE / PRIVATE / PRESENTATION / ROUND / CAN_SHOW_WITH_INSECURE_KEYGUARD / SHOULD_SHOW_SYSTEM_DECORATIONS / TRUSTED). Captures non-PRESENTATION displays previously hidden by D2's filter. **D22 — Display #2 owner identification.** `dumpsys display 2>/dev/null | grep -E 'Display id|mDisplayId|mOwnerUid|mOwnerPackageName|mType|mFlags|mName'` + `dumpsys SurfaceFlinger --display-id` + `ps -A -o PID,USER,NAME` to cross-reference the PID 1326 owner spotted in D14. **D23 — auto_container transaction codes scan.** Loop over codes 0..8 with `service call auto_container <N> i32 0 i32 0 s16 \"\" 2>&1` — code 0 = `INTERFACE_TRANSACTION` (returns interface descriptor), code 2 = `sendInfo` (known). Maps the full binder API surface to discover hidden capabilities; counts how many codes return `Result: Parcel` (= accepted). **D24 — BYD-specific services probe.** `service check` + `service call <svc> 0` (INTERFACE_TRANSACTION) on 11 services: `magicwindow`, `crossservice`, `mirror`, `BYDMgmt`, `byd_datacached`, `IBYDCDRService`, `DevOperatorService`, `autoservice`, `byd_updated_service`, `cloud_server_app_service`, `color_display` — confirms aliveness and captures each interface descriptor. `magicwindow` (`android.view.IMagicWindowManager`, id 140 on DL5) is the prime suspect for the dual-screen/cluster app windowing API. **D25 — IATM methods enumeration.** Reflective dump of every declared method on `android.app.IActivityTaskManager`, partitioned into a `HOT` bucket (names containing windowing/display/task/launch/start/move) and `OTHER`. Each line includes parameter types and return type — finds the actual signature that replaces the absent `setTaskWindowingMode(int,int,boolean)` on this build. **D26 — `am start --display N` probe on each non-main display.** For each display id ≠ 0 (force-includes id 2): force-stop StandardADB → `am start --display N -a MAIN -c LAUNCHER -n com.github.standardadb/.SplashActivity` → sleep 800 ms → `dumpsys activity activities | grep -E 'mResumedActivity|displayId|com.github.standardadb'` to see where it actually landed. Reveals which displays accept arbitrary apps without sendInfo preparation. **D27 — BYD `clusterdebug` app launch.** Resolve `com.byd.clusterdebug/.MainActivity` via `cmd package resolve-activity --brief` + manifest dump, then `logcat -c` → `am start` → sleep 1500 ms → activity-stack check → `logcat -d | grep -iE 'clusterdebug|cluster|auto_container|AutoContainer|projection|fission' | tail -40` → force-stop. This is BYD's own cluster diagnostic tool, will reveal the legitimate projection flow as exercised by BYD's QA. **D28 — Live logcat capture during sendInfo cycle.** `logcat -c` → `sendInfo(1000, 30)` (screen-size hint) → sleep 2 s → `sendInfo(1000, 16)` (projection ON) → sleep 3 s → `dumpsys display` post-sendInfo → `logcat -d -v threadtime | grep -iE 'auto_container|AutoContainer|cluster|projection|fission|XDJA|magicwindow|MagicWindow|containerservice' | tail -80` → cleanup `sendInfo(1000, 18)` + `sendInfo(1000, 0)`. Captures every BYD log line emitted during a real projection-start attempt — will reveal which component rejects/accepts and why. **D29 — Projection-related intent filters discovery.** `cmd package query-intent-activities -a '<action>'` + `cmd package query-intent-services -a '<action>'` for 9 candidate actions (`com.byd.action.PROJECT`, `com.byd.action.CLUSTER_PROJECTION`, `com.byd.containerservice.PROJECT/START`, `com.byd.appstartmanagement.START`, `com.byd.appstartup.START`, `com.byd.intent.action.CLUSTER`, `com.byd.cluster.START`, `byd.intent.action.AUTO_DISPLAY`) + full intent-filter dump (`Action:`, `Category:`, `Scheme:`) of `containerservice` + `appstartmanagement`. Enumerates every intent BYD apps declare for projection so we can pick the official entry point. **D30 — SurfaceFlinger display topology.** `dumpsys SurfaceFlinger --display-id` + filtered `dumpsys SurfaceFlinger | grep -E 'Display |layerStack|Output|orientation|hwc composition|HWComposer'` + top 40 layers — ground-truth physical/virtual display mapping from the compositor (bypasses WindowManager filtering). **Catalog expanded 20 → 30 tests**, switch dispatcher extended with 10 new `case` lines, 10 new run methods (+~250 LoC) inserted before the helpers section, no new dependencies (reuses existing `runShellSync`). No production-path changes — read-only reconnaissance only; the cluster cleanup at the end of D28 ensures the device is left in a clean state. **Build.** `versionCode` 182 → 183, `versionName` pinned `"1.2.0"`. Sole touched file outside CHANGELOG / gradle: `DiLink5TestRunner.java` (catalog 20 → 30 entries, switch extended, 10 new private runD21..runD30 methods). **Validation plan.** Push to a DL5 testeur, ask them to (a) install the OTA / sideload manually, (b) reboot to ensure fresh state, (c) open Diag → DiLink 5 → tap `Run all` → wait for the full 30-test pass (~60 s), (d) `Copy report` → share the `.log` via Telegram. Combined with the 7 APKs already extracted by build 182 (offline jadx work in progress), the new D21–D30 data will let us design the proper fix: platform-aware service name selection (`auto_container` on DL5 vs `AutoContainer` on DL3) + the correct windowing API for DL5 (likely via `magicwindow` or a new IATM method discovered by D25 / D29) + correct target display identification (D21 / D22 will resolve the display-#2 mystery). |
| **1.2.0-build182** | 182 | **DiLink 5 enablement — prerelease branch (`beta/1.2.0-dilink5`).** Major version bump because DL5 is a different platform (Android 12 / API 32, BYD-AUTO brand, new `com.byd.containerservice` rebrand, new `com.byd.appstartmanagement` gatekeeper) and the diagnostic surface required to triage it is non-trivial. **DiLink 5 diag — 11 new tests (D10–D20) + D9 dynamic discovery + APK extraction (whitelisted) for reverse-engineering.** First DL5 field log (`byd_report_20260521_212356.log`, API 32, BYD-AUTO) revealed three actionable insights: (1) `auto_container [android.os.IAutoContainer]` is still present on DL5 — our typed `Phase4Verbs.autoContainerSendInfo` path may work as-is; (2) display naming is the same `shared_fission_bg_XDJAScreenProjection_N` family as DL3; (3) the new gatekeepers `com.byd.appstartmanagement 1.5.0.0` + `com.byd.providers.appstartup 12` + the BYD-rebranded `com.byd.containerservice 12` (replacing `com.xdja.containerservice`) are the suspected behavioural divergence. Build 182 expands the DL5 diagnostic suite to triage all three threads. **(1) D9 reworked — dynamic discovery.** Replaces the 7-package hardcoded whitelist with a shell scan: `pm list packages -f \| grep -iE 'com\.byd\.\|com\.xdja\.\|com\.dilink\.\|cluster\|automotive' \| sort -u`. Parses every line (format `package:/data/app/.../base.apk=<pkg>`), reads `versionName` + `getLongVersionCode()` (API 28+) via PackageManager, and stores the result in a static `sLastDiscovery` list of `DiscoveredPkg` records for downstream tests. Output now shows the full APK path on disk for each match. Status WARN if zero matches, PASS otherwise. **(2) D10 — APK extraction for reverse-engineering.** Iterates `sLastDiscovery`, `cat`s each `apkPath` from `/data/app/...` to `getExternalFilesDir(null)/extracted_apks/<pkg>_v<versionCode>.apk` via the existing `AdbLocalClient` shell relay (uid 2000 has read access to world-readable APK files; `cat` redirect avoids `cp` permission quirks observed on some BYD builds). On success, the per-file `ls -l` is logged plus file size in KB. Final report card shows the target dir path with the exact `adb pull` command to retrieve everything in one shot. SKIPPED if D9 was not run first (empty discovery). **(3) D11 — AutoContainer sendInfo refresh probe.** `service call auto_container 2 i32 1000 i32 0 s16 ""` (the harmless `sendInfo(0)` refresh that's already part of our restoreBydOnCluster sequence). Pattern-matches the output for `SecurityException` / `Permission Denial` / `Result:` / `Parcel`. PASS = service accepted from uid 2000 (confirms our typed Phase 4c path will work on DL5 without modification). FAIL = SecurityException or service-not-found. **(4) D12 — Qt standby cycle probe.** Active 3-step sequence `sendInfo(16)` (Qt standby) → sleep 1.5s → `sendInfo(18)` (close) → sleep 0.5s → `sendInfo(0)` (refresh). Validates the *protocol cycle*, not just the binder reachability. No projection actually started — these three commands together form a complete null cycle. PASS if no SecurityException / no service-not-found anywhere in the 3-step output. **(5) D13 — IATM.setTaskWindowingMode reflection probe.** Resolves `android.app.ActivityTaskManager.getService()` then reflective `setTaskWindowingMode(0, 4, true)` (taskId=0 = bogus on purpose). Three outcomes: `InvocationTargetException(SecurityException)` → WARN (expected on un-signed app, confirms we need signature perms or proxy daemon path); `InvocationTargetException(IllegalArgumentException)` → PASS (method is reachable, only rejected the bogus taskId — best case, means the API is usable once we have a real taskId); `NoSuchMethodException` → FAIL (method removed). Disambiguates the D6 FAIL line. **(6) D14 — Window manager state dump.** `dumpsys window \| grep -E 'Display #\|mDisplayId\|imeLayerStack\|focused\|mStackId\|mCurrentFocus\|mDisplayContent' \| head -80`. Captures the live windowing state without flooding the report. Useful as a baseline snapshot before reproducing a cluster bug. **(7) D15 — `appstartmanagement` package inspection.** `dumpsys package com.byd.appstartmanagement` filtered to permission/receiver/service/activity/authority/provider/signature/version lines, capped at 120. This is the suspected DL5 launch gatekeeper — the inspection will show which intents/categories/actions it claims and which protectionLevel its custom permissions use. **(8) D16 — `containerservice` package inspection.** Same filter applied to `com.byd.containerservice` (the BYD-rebranded XDJA). Will reveal whether the wake/release transaction codes are the same as the old `com.xdja.containerservice` we have in `decompiled/`. **(9) D17 — proxy daemon spawn capability.** Three-step shell probe: `id` (confirm shell uid) → `which app_process64 app_process` (locate the spawn target) → `app_process64 -Xnoimage-dex2oat /system/bin --nice-name=dl5_probe -version` (actually try to spawn it with our daemon's command flags, capturing the first 5 lines of output) → `ls /system/bin/app_process64`. PASS = app_process64 reachable with no permission denial / no `avc:` (SELinux) lines — our Beta proxy daemon strategy is viable on DL5/API 32. FAIL = SELinux denied. WARN = binary not at expected path. **(10) D18 — SELinux context.** `id` + `ps -Z -o LABEL,USER,PID,NAME \| head -10`. Gives the SELinux domain of the shell user and the first 10 processes, needed to diagnose SELinux-related daemon spawn failures. **(11) D19 — Full system services inventory.** Broader than D3: `service list \| grep -iE 'auto\|byd\|cluster\|fission\|display\|window\|projection\|crosscontrol\|xdja\|task' \| head -60`. Captures the full landscape of cluster-relevant binders — useful for spotting BYD-specific services we don't know about yet. **(12) D20 — IActivityTaskManager service presence.** `service list \| grep -iE 'activity_task\|activitytask'`. Confirms the binder exists at the OS level (required precondition for the D13 typed path). **(13) Discovery handoff between tests.** D9 stores its results in `private static final List<DiscoveredPkg> sLastDiscovery` (cleared at the start of each D9 run, so re-running just the suite gives a fresh capture). D10 consumes it and SKIPs if empty. This is the cleanest cross-test data flow without polluting the `TestResult` data class. **(14) Behavioural contract preserved.** No public API changed; existing tests D1–D8 untouched. The `D8Params` holder, the listener contract, and the report format are unchanged. D10's extraction directory is `Context.getExternalFilesDir(null)/extracted_apks/` — falls back to internal `filesDir` if external is unavailable. All new tests use the existing `runShellSync` helper with appropriate timeouts (4–8 s for inspection, 15 s for D10's APK copy loop). **(15) UI / panel.** No change required — the DL5 panel already iterates `DiLink5TestRunner.catalog()` programmatically and inflates one row per `TestDef`. The 11 new entries surface automatically as new rows in the existing scrollable list. **(16) Build.** `versionCode` 181 → 182, `versionName` pinned at `"1.1.9"`. Touched files: `app/build.gradle` (versionCode), `app/src/main/java/com/byd/dashcast/dilink5/DiLink5TestRunner.java` (catalog 9 → 20 entries, switch extended, runD9 rewritten, 11 new run methods + `DiscoveredPkg` holder — +~270 LoC). No new strings, layouts, drawables, dependencies. **(17) Validation plan.** Push to a DL5 testeur, ask them to (a) open Diag → DiLink 5 tab → tap `Run all`, (b) capture the report via the `Copy` button — expected: PASS on D9 with 10–20 packages discovered, PASS on D10 with all APKs in extracted_apks/, PASS or WARN on D11/D12 (the key signal for whether our typed sendInfo path works), useful detail on D13–D20 for triage; (c) `adb pull /sdcard/Android/data/com.byd.dashcast/files/extracted_apks/` from a PC to retrieve all BYD APKs for offline jadx work. |
| **1.1.9-build181** | 181 | **DiagActivity — restore RE Sniffer as a dedicated Material 3 tab.** The Sniffer system (continuous `logcat` + periodic `dumpsys` snapshots into a single `BYD_RE_Sniffer_*.txt` file on the external files dir) was wiped at v0.9.88 (commit `229059c`) when DiagActivity was refactored into the tabbed layout. Only the file-name reference survived in `AppLogger` (storage-cleanup glob). With new Dilink5 testers sending in field logs, we need the Sniffer back to bound user-reported issues — but plugged into the new M3 tab structure rather than the old stacked sections. **(1) New tab `Sniffer` (position 7).** Added `TAB_SNIFFER = 7` constant in `DiagActivity`, new `<TabItem>` in `activity_diag.xml` after `dilink5`, panel toggled in `showPanelForTab` alongside the existing isBeta/isDl5 branches. **(2) New layout `include_diag_sniffer.xml`.** Three stacked M3 cards inside a `NestedScrollView`, identical chrome to `include_diag_beta_engine.xml` (20 dp corner radius, `md_outline_variant` stroke, `md_surface_container_low` background, 20 dp content padding): (a) **Header/status card** — icon + title + subtitle + status pill (`ACTIF` green / `INACTIF` red) + monospace status line + 4 `MaterialButton`s (`Démarrer` filled, `Arrêter` tonal, `Snapshot` outlined, `Exporter` outlined). (b) **"Contenu capturé" info card** — bullet list explaining what lands in the file (header, live logcat threadtime + events, auto snapshots every 10 s of displays/SF/processes, manual snapshot adds `dumpsys window` + broadcasts history, persistence via setsid). (c) **"Procédure type" info card** — 5-step usage flow ending on "Exporter → Telegram". Labels are hardcoded French with `tools:ignore="HardcodedText"` — power-user diagnostic tool, the 12-locale i18n cost was deemed not worth paying for this surface. **(3) Sniffer state machine restored 1:1 from pre-wipe code (`DiagActivity.java` @1160–1500 at commit `229059c^`).** Constants `RE_SNIFFER_TAG = ".re_sniffer_run"`, `RE_SNIFFER_PIDS = ".re_sniffer_pids"`, `RE_SNIFFER_PREFIX = "BYD_RE_Sniffer_"`, prefs key `byd_diag_prefs / re_sniffer_file_path`. Start path: `logcat -c` + create tag file + write header (date, `getprop ro.product.model`, fingerprint, dumpsys display, dumpsys SurfaceFlinger, ps -A), then `setsid sh -c '…'` × 3 (logcat threadtime, snap loop every 10 s, logcat events buffer) with PIDs captured to `.re_sniffer_pids` for clean teardown. The `setsid` is load-bearing: `nohup` alone doesn't survive adbd `SIGHUP` to the process group. Stop path: removes tag file (snap loop exits at next iteration), reads PID file and `kill -9` each PID, `pkill -f BYD_RE_Sniffer_` fallback. Snapshot path: appends `dumpsys display` + `dumpsys window` + `dumpsys SurfaceFlinger` + `ps -A` + `dumpsys activity broadcasts history` with a `=== SNAPSHOT MANUEL HH:MM:SS ===` separator. Export path: `FileProvider.getUriForFile` + `Intent.ACTION_SEND` with `text/plain` + `FLAG_GRANT_READ_URI_PERMISSION` + `ClipData.newRawUri` for chooser compatibility on API 29. **(4) Persistence across Activity recreation.** `startSniffer` writes the file path to `SharedPreferences(PREF_SNIFFER)` immediately, `stopSniffer` clears it. New `restoreSnifferState()` runs in `onCreate` after `bindSnifferPanel`: reads the saved path, checks the file exists, then runs `[ -f /data/local/tmp/.re_sniffer_run ] && echo ACTIVE || echo STOPPED` via `AdbLocalClient.executeShellWithResult` and restores the UI to ACTIF (green pill, Stop/Snapshot/Export enabled) or "Capture précédente arrêtée — exportable" (Export still enabled because the file is on disk). Covers the case where the user rotates / leaves the activity while a capture is running — they come back and find it still going. **(5) Local helper `safeRunOnUiThread`.** The pre-wipe code used a class-wide `safeRunOnUiThread` that checked `mDestroyed` before delivering. The new DiagActivity didn't have it (Beta Engine panel uses runner-side guards). Added a minimal copy at the bottom of `DiagActivity.java`: skips delivery if `mDestroyed`, also rechecks inside the `runOnUiThread` lambda to cover the race window. Used by all four Sniffer callbacks. **(6) Behavioural contract.** No public API change. No new permissions (`adb_local` shell already has everything the Sniffer needs — it was always running uid 2000 since v0.x). The capture is fully opt-in (button-driven) and survives reboot **only as a stale file** on disk: the tag file lives in `/data/local/tmp` and is wiped on reboot, so the `restoreSnifferState` post-reboot check correctly reports "Capture précédente arrêtée" and clears the prefs. **(7) Wire format unchanged.** No daemon work, no new typed verb — Sniffer uses the legacy `AdbLocalClient.executeShell` / `executeShellWithResult` paths exclusively. Could be migrated to `Phase4Verbs.runShell` later for ~50 ms / call saving, but not on a hot path and the shell relay is already plenty fast for a manual user action. **(8) Build.** `versionCode` 180 → 181, `versionName` pinned at `"1.1.9"`. Touched files: `app/build.gradle` (versionCode bump), `app/src/main/res/layout/activity_diag.xml` (1 new TabItem + 1 new `<include>`), `app/src/main/res/layout/include_diag_sniffer.xml` (new, 178 lines), `app/src/main/java/com/byd/dashcast/DiagActivity.java` (TAB_SNIFFER constant + panelSniffer field + 3 surgical edits in bindTabs/showPanelForTab/onCreate + ~270 LoC appended for bindSnifferPanel/setSnifferUiActive/buildSnifferFile/restoreSnifferState/startSniffer/killSnifferProcesses/stopSniffer/snapshotSniffer/exportSniffer/safeRunOnUiThread). No new dependencies, no new drawables (reuses `ic_stethoscope`), no new string resources, no new permissions. **(9) Validation plan.** (a) Open DashCast → Diag → tap `Sniffer` tab → confirm the 3 M3 cards render at 1920×1080 without horizontal scroll and the status pill reads "INACTIF" red. (b) Tap `Démarrer` → toast `Sniffer démarré → BYD_RE_Sniffer_<ts>.txt` → pill flips to "ACTIF" green → status line shows the file name. (c) Reproduce a known bug (ex. project Waze, restore cluster) → tap `Snapshot` → toast `Snapshot ajouté.` → toggle to MainActivity and back to Diag → confirm UI state restored to ACTIF (this validates `restoreSnifferState`). (d) Tap `Arrêter` → pill flips to INACTIF red, status line shows "Arrêté → … Prêt pour export.", Export button stays enabled. (e) Tap `Exporter` → Telegram chooser opens with `BYD_RE_Sniffer_*.txt` attached → forward to dev. (f) Cold-reboot the head unit, reopen DashCast → Diag → Sniffer → status line should read "Capture précédente arrêtée → … exportable." with Export enabled and Start enabled (tag file gone after reboot, file still on disk). |
| **1.1.9-build180** | 180 | **Beta Engine Phase 4d.1 — fix silent `forceStopPackage` no-op + post-kill verification.** Bug surfaced by build-179 field test: when the user stopped a projection (Waze on cluster), the app correctly migrated to display 0 (`moveSessionAppsToMainDisplay` works) but the process was **not** killed despite the device log reporting `beta restoreBydOnCluster typed ok (Xms)`. Root cause: `BetaProxyClient.forceStopPackage(targetPackage, -1)` was passing `userId = -1` (`UserHandle.USER_ALL`), which is silently no-op on the API 29 BYD framework's `IActivityManager.forceStopPackage(String, int)` implementation — the call returned without throwing but did not actually terminate the target process. The shell legacy fallback (`am force-stop pkg` without `--user`) targets the current user (0) implicitly, which is why pre-Phase-4d builds did not exhibit this. **(1) Fix.** Both `AdbLocalClient.restoreBydOnCluster` and `AdbLocalClient.restoreOriginCluster` now pass `userId = 0` (current user) instead of `-1`. The log line now reads `force-stop <pkg> (typed,u=0)` to make the user-id explicit in the report shown by the UI. Two-line change per call site. **(2) Post-kill verification (`verifyForceStop` helper).** After every typed `forceStopPackage` on the teardown hot path, the client now calls `BetaProxyClient.getPidsByPackage(pkg)` (Phase 4b verb, already in production since build 175) and logs `WARN beta force-stop ineffective for <pkg> (pids=<list>)` when surviving PIDs are detected, plus appends `WARN: still alive, pids=…` to the UI report. On success the report shows `verified killed`. Failures inside the verification helper are swallowed (logged at WARN) so the teardown sequence is never broken by a diagnostic. **(3) Why not throw + escalate to shell.** Tempting but counter-productive: the shell fallback uses the same default-user semantics as the new typed userId=0, so if force-stop still doesn't bite the cause is upstream (Android re-launching the app from a sticky service, or BYD framework with a non-standard force-stop hook). Phase 4d.1 prefers visibility over silent retry — the WARN line makes the failure mode obvious in the next device log so we can investigate the real root cause if it recurs. **(4) Out of scope (deferred).** `forceStopApp(Context, String)` (shell-only legacy path, used by the in-list "✕" button) is **unchanged** — that path runs `am force-stop` directly, no userId issue, and includes its own `am task remove` pre-pass for cleaning up Recents. Per-stack `Process.killProcess(pid)` as a third escalation tier was considered and explicitly deferred — depends on whether the new WARN log shows a recurrence at all. **(5) Wire-protocol.** No change. TXN_FORCE_STOP_PACKAGE (=8) and TXN_GET_PIDS (=6) were both shipped in build 178/175 respectively; Phase 4d.1 is a pure client-side correctness fix. PROTOCOL_VERSION stays `"2"`. **(6) Behavioural contract preserved.** With beta proxy toggle OFF, neither branch is entered (`BetaConfig.isProxyDaemonEnabled(context)` short-circuits) — identical to v1.0.1. The legacy shell path inside `restoreBydOnCluster` / `restoreOriginCluster` (used as fallback when typed throws) is unchanged. **(7) Validation plan.** After install + reboot: (a) project Waze (or any app) to the cluster, (b) tap the "Restore BYD cluster" button, (c) verify in the in-app Diag log that the report contains `force-stop <pkg> (typed,u=0)` followed by `verified killed`, (d) confirm visually that the app is no longer running on display 0 (launcher returns to its idle state, not the freshly-relocated app). If the WARN line appears instead, capture the log and report — that means the userId fix alone wasn't enough and we need to investigate Android's re-launch behaviour on this BYD framework variant. **(8) Build.** `versionCode` 179 → 180, `versionName` pinned at `"1.1.9"`. Touched files outside CHANGELOG/gradle: `AdbLocalClient.java` (three small edits: two `userId` changes + one `verifyForceStop` helper). No new strings, layouts, drawables, dependencies, or protocol verbs. |
| **1.1.9-build179** | 179 | **Beta Engine Phase 5 — proxy daemon pre-warm at `BOOT_COMPLETED`.** Kills the cold-start cost paid by the first user action after every reboot. Build-178 device log measured the very first `sendInfo` of a fresh session at **1916 ms** (versus 3 ms warm) — that 1.9 s was entirely the `BetaProxyClient.connect()` bootstrap: spawning `app_process` with our APK on the classpath, waiting for the daemon to broadcast its live binder, running the WHOAMI handshake. Build 178 already validated the runtime cost is paid once per daemon lifetime, so moving the bootstrap to BOOT means every subsequent in-app trigger is warm. **(1) `BootReceiver` refactor.** The existing `BOOT_COMPLETED` receiver already handled two cases (auto-start projection ON → start `ClusterService`; OFF → run `cleanupDisplayAffinityAtBoot` via `goAsync()`). Phase 5 adds a third orthogonal task: if `BetaConfig.isProxyDaemonEnabled(context)` is true, spawn `beta-daemon-prewarm` thread that calls `BetaProxyClient.connect(appCtx)`. Since `BroadcastReceiver.PendingResult.finish()` may only be called **once**, the previous single-task `goAsync()` pattern doesn't scale to two concurrent tasks (pre-warm + display cleanup) — refactored into a shared `AtomicInteger pending` + `releaseOne` lambda that finishes the receiver only after the last task completes. The pre-warm thread name is `beta-daemon-prewarm` (visible in any thread dump for diagnostics). **(2) Logging on the boot path.** Two new log lines surface the pre-warm outcome: `beta daemon pre-warm starting...` at thread start, then `beta daemon pre-warm ok (XXms)` / `beta daemon pre-warm FAILED (XXms)` once `connect()` returns. Expected `XX` is ~1500-2000 ms on the BYD Seal EU (matches the cold cost previously paid by the user). On failure the log line is `beta daemon pre-warm threw: <msg>` and Phase 5 silently degrades — runtime fallback in `ShellGateway` / `AdbLocalClient` still routes through legacy shell on demand, so the user's first action just pays the cold cost as before. **(3) Threading correctness.** The pre-warm runs on its own background thread (not the receiver thread that delivers the broadcast — that one returns immediately so the OS can move on with system boot). `BetaProxyClient.connect()` is internally synchronized on `LOCK` and is safe to call concurrently with future in-app calls from `MainActivity.onCreate` etc. — if the user opens the app *while* pre-warm is still in flight, the in-app `connect()` will block on the same `LOCK`, see the now-cached binder on entry, and return immediately. **(4) Idempotent against double-trigger.** `BetaProxyClient.connect()`'s fast-path explicitly checks `isConnected()` first and skips the bootstrap if a live binder is already cached (this was the v1.1.6 anti-respawn fix), so a pre-warm followed by an `MainActivity.onCreate()` connect is harmless — second call is a no-op. **(5) Receiver-lifetime safety.** `PendingResult.finish()` must be called eventually or Android logs a leak. The new `AtomicInteger`-based coordinator guarantees finish is called exactly once, on the completion of whichever task finishes last (pre-warm + cleanup typical max ~3 s). If `BetaConfig.isProxyDaemonEnabled(context)` returns false AND `autoStartEnabled` is true, `pending` stays at zero and finish is invoked immediately — covers the edge case where the receiver has no asynchronous work to do. **(6) Permissions / manifest.** No change. `RECEIVE_BOOT_COMPLETED` was already granted (build-178 SigDump confirms `granted=true`); the `<receiver>` declaration with the `BOOT_COMPLETED` action filter is unchanged from build 178. **(7) Behavioural contract preserved.** When the beta proxy toggle is OFF, the new `if (BetaConfig.isProxyDaemonEnabled(context))` branch is skipped entirely — no extra thread, no daemon bootstrap, identical boot behaviour to v1.0.1. The existing `auto-start projection` path is unchanged in terms of order: `startForegroundService` runs synchronously on the receiver thread as before; only the cleanup branch lost its dedicated `goAsync()` (it's now sharing the receiver's single PendingResult). **(8) What's left.** Phase 5 closes the cold-start hole exposed by Phase 4c/4d logs. Toggling the proxy ON at runtime (via Settings) still requires the user to pay the cold cost on the first call — by design, since the toggle change is rare and the UI already labels it "restart required after change". A future micro-polish could trigger `BetaProxyClient.connect(ctx)` in the toggle handler too, but it's a deliberate non-scope of Phase 5 (boot is the dominant cold-start surface). **(9) Build.** `versionCode` 178 → 179, `versionName` pinned at `"1.1.9"` per channel policy. No new strings, layouts, drawables, dependencies. Sole touched file outside CHANGELOG/gradle: `BootReceiver.java`. |
| **1.1.9-build178** | 178 | **Beta Engine Phase 4d — typed `IActivityManager.forceStopPackage` + typed `AutoContainer.transact(2)` cover the projection teardown sequence. Bundled fix: `BetaTestRunner` B4/B5/B6 wired to the real SDK package present in `byd-auto-api-stubs.jar` (`android.hardware.bydauto.{bodywork,radar}.*`) instead of the phantom `com.byd.bydautosdk.*` that never existed in this build — B4/B5/B6 will now actually run instead of always returning `SKIPPED: … not on classpath`.** Closes the gap left by build 177: `AdbLocalClient.sendInfo` was migrated to the typed path, but `restoreBydOnCluster` and `restoreOriginCluster` still issued their own `dadb.shell("am force-stop …")` and `dadb.shell("service call AutoContainer 2 …")` forks directly, bypassing the centralised `sendInfo` hook. Build 177's device log (`byd_log_20260521_234340.log`) confirmed the gap: every projection stop printed `restoreBydOnCluster -> OK` with 3 shell forks (1 force-stop + 2 sendInfo) on the teardown hot path. **(1) `Phase4Verbs.forceStopPackage(String packageName, int userId) throws Throwable`** (~50 new LoC). Lazy double-checked cache: `sActivityManager` (via reflective `ActivityManager.getService()` / fallback `ActivityManagerNative.getDefault()`) + `sForceStopPackage` (Method). Probe P3 (build 173) confirmed `IActivityManager.forceStopPackage(String, int)` is callable from uid 2000 on the BYD Seal EU. Pass `userId = -1` for `UserHandle.USER_ALL` (matches legacy `am force-stop` default). **(2) `ProxyDaemonMain.TXN_FORCE_STOP_PACKAGE = FIRST_CALL_TRANSACTION + 7` (=8).** Additive transaction; protocol version stays `"2"`. Wire: `String packageName, int userId` in → empty reply (or `writeException` on failure). The onTransact case mirrors the Phase 4c pattern: unwraps `InvocationTargetException` so the client receives the real underlying exception, wraps non-`Exception` throwables in `RuntimeException` so `writeException` accepts them. **(3) `BetaProxyClient.forceStopPackage(String, int) throws BetaProxyException`** — mirrors the established Parcel pattern. `RemoteException` nulls the cached binder so the next call triggers a fresh connect. **(4) `AdbLocalClient.restoreBydOnCluster` — typed-first dispatch for the whole sequence.** New top-of-`run()` block (same pattern as Phase 4c `sendInfo`): if `BetaConfig.isProxyDaemonEnabled(context)`, run the full typed flow (`forceStopPackage(targetPackage, -1)` + sleep 500 ms + `autoContainerSendInfo(1000, 18, "")` + sleep 1 s + `autoContainerSendInfo(1000, 0, "")`), log `beta restoreBydOnCluster typed ok (Xms)`, hand the success message to the callback, return. On any `Throwable` other than `InterruptedException`, log `beta restoreBydOnCluster typed failed after Xms, falling back to ADB shell: <msg>` and fall through to the unchanged legacy `try (Dadb dadb = connect(context)) { … }` block. `InterruptedException` short-circuits to `callback.onError("interrupted")` (don't re-fork shell when the caller cancelled). **(5) `AdbLocalClient.restoreOriginCluster` — same typed-first treatment.** Full typed sequence: force-stop (typed) + 6 s sleep + sendInfo(18) (typed) + 6 s sleep + sendInfo(0) (typed) + 6 s sleep + sendInfo(screenSizeCmd) (typed). Identical fallback policy. The 12 s of `Thread.sleep` between sendInfo calls is deliberately preserved — they exist because the Qt cluster process needs time to release/reacquire its surface, not because of shell overhead. **(6) Why "whole-flow" fallback rather than per-step.** Both flows interleave binder calls with `Thread.sleep` that have load-bearing semantics. If we fell back step-by-step, a typed `forceStopPackage` followed by a shell `service call` would race with the daemon's binder state. By committing to the whole flow up-front (typed or shell), we never mix the two within one teardown, and either fallback path is one we already shipped and validated. **(7) Sites that benefit automatically.** `restoreBydOnCluster` is the end-of-every-projection teardown (triggered by the user's exit button, by app force-stop detection in `reconcileDisplayState`, and by `BYDApp.moveSessionAppsToMainDisplay`). `restoreOriginCluster` is the "reset cluster to native size" path (Settings → Cluster size → Apply). With Phase 4d, both stop forking shells entirely when the proxy is enabled. **(8) Wire-protocol summary so far.** TXN_PING=1, TXN_WHOAMI=2, TXN_EXEC=3, TXN_PROBE_PHASE4=4, TXN_SET_OVERSCAN=5, TXN_GET_PIDS=6, TXN_AUTOCONTAINER_SEND_INFO=7, **TXN_FORCE_STOP_PACKAGE=8**. PROTOCOL_VERSION still `"2"` — every Phase 4 verb has been purely additive, so the v1.0.1 wire format remains a strict subset. **(9) What's left after this build.** With Phase 4d shipped, every shell call on a complete projection lifecycle (start → run → stop → restore) is typed when the proxy is enabled. The remaining `dadb.shell(…)` calls in `AdbLocalClient` are all explicitly diagnostic/self-test: `runDisplayOneLaunch` (Diag button "Test 4-sendInfo sequence"), `sendInfoDiagDump` (Diag dump for OEM compatibility checks), `dumpSignatureAndPermissions` (one-shot debug button), `setAccWhitelist`/`grantPermission`/`killAndRelaunchDaemon` (admin tools, fire once per install). None are on a hot path. **(10) Behavioural contract preserved.** No public API removed, no signature change. With beta proxy toggle OFF both restore paths still call `dadb.shell` exactly as in v1.0.1 (the `BetaConfig.isProxyDaemonEnabled(ctx)` check short-circuits before any binder touch). **(11) Build.** `versionCode` 177 → 178, `versionName` pinned at `"1.1.9"` per channel policy. No new strings, layouts, drawables, dependencies. |
| **1.1.9-build177** | 177 | **Beta Engine Phase 4c — typed `AutoContainer.transact(2)` replaces the `service call AutoContainer 2 …` shell relay used by `AdbLocalClient.sendInfo`.** Third (and final hot-path) production cash-in of the Phase 4 probe matrix. Build-176 P13 measured the direct transact at **0 ms** on the BYD Seal EU and confirmed both that uid 2000 is accepted by the service and that the live descriptor is `android.os.IAutoContainer`. **(1) `Phase4Verbs.autoContainerSendInfo(int type, int info, String str)`** (~85 new LoC across the file). Lazy double-checked cache: `sAutoContainerBinder` (IBinder) + `sAutoContainerDescriptor` (String). The descriptor is **read at runtime** via `INTERFACE_TRANSACTION` rather than hard-coded, so a future OEM rebrand (descriptor renamed but transaction code unchanged) still goes through — robustness that probe P13 specifically validated. Cache invalidation: `IBinder.pingBinder()` is checked on every entry, and a `false` return triggers a fresh `ServiceManager.getService("AutoContainer")` resolve. Parcel wire format mirrors the legacy `service call`: `writeInterfaceToken(liveDescriptor)` + `writeInt(type)` + `writeInt(info)` + `writeString(str)`. `readException()` surfaces any remote `SecurityException` / `IllegalArgumentException` so the client falls back to the shell wrapper with full diagnostic context. **(2) `ProxyDaemonMain.TXN_AUTOCONTAINER_SEND_INFO = FIRST_CALL_TRANSACTION + 6` (=7).** Additive transaction; protocol version stays `"2"`. Wire: `int type, int info, String str` in → empty reply (or `writeException` on failure). The onTransact case unwraps `InvocationTargetException` so the client receives the real underlying exception rather than a generic reflection wrapper. **(3) `BetaProxyClient.autoContainerSendInfo(int, int, String) throws BetaProxyException`** — mirrors the established `runShell` / `setOverscan` / `getPidsByPackage` Parcel pattern. `RemoteException` nulls the cached binder so the next call triggers a fresh connect. `null` str is normalised to `""` on the wire, matching the legacy shell wrapper's behaviour. **(4) `AdbLocalClient.sendInfo(...)` — typed-first dispatch.** This is the **option B** integration: rather than migrating every individual `service call AutoContainer 2 …` site to `ShellGateway`, we hook the single private API that already centralises the call. New top-of-`run()` block checks `BetaConfig.isProxyDaemonEnabled(context)`; if ON it tries `BetaProxyClient.autoContainerSendInfo(...)` after lazy `connect(context)`, logs `beta sendInfo typed ok (Xms): type,info,"str"` on success and invokes `callback.onSuccess("")` — empty string matches what every existing caller expects (none of them parses the Parcel hex dump that the legacy shell relay returned). On `Throwable`: logs `beta sendInfo typed failed after Xms, falling back to ADB shell: <msg>` and **falls through** to the unchanged legacy `try (Dadb dadb = connect(context)) { … }` block. Fallback chain is therefore **typed daemon → legacy ADB shell relay** (one hop simpler than the gateway chain because there is no intermediate `runShell` step — the entire legacy implementation is reused as-is). **(5) Sites that benefit automatically.** All call sites of `AdbLocalClient.sendInfo(ctx, type, info, str, cb)` migrate transparently: `ClusterService.startActivityViaIAM` (sendInfo(0) refresh Qt stream, sendInfo(18) close projection, sendInfo(30) Seal EU screen size, sendInfo(16) Qt standby), `MainActivity.toggleClusterProjection`, the projection stop sequence in `ClusterService.stopProjectionNoAdb`, and the diagnostic dump in `SysInfoActivity`. The Diag → ADB → "Tester sendInfo" button keeps calling `dadb.shell("service call AutoContainer 2 …")` directly (intentional, exercises the legacy path for comparison). **(6) Latency expectations.** Build-176 P13 measured the typed transact at 0 ms. Production calls will add ~30 ms of binder round-trip (daemon → client) so total ~30 ms vs the legacy ~100–300 ms (forks `service`, parses its hex output, escapes the double-quoted `s16` argument). Most user-visible win: projection start/stop sequence now does ~6 sendInfo calls back-to-back — the saving compounds. **(7) Wire-protocol summary so far.** TXN_PING=1, TXN_WHOAMI=2, TXN_EXEC=3, TXN_PROBE_PHASE4=4, TXN_SET_OVERSCAN=5, TXN_GET_PIDS=6, **TXN_AUTOCONTAINER_SEND_INFO=7**. PROTOCOL_VERSION still `"2"` — every Phase 4 verb has been purely additive, so an old client (pre-build 174) still works with the new daemon and vice-versa modulo the missing verbs. **(8) Behavioural contract preserved.** No public API removed, no signature change in `AdbLocalClient.sendInfo`. With beta proxy toggle OFF the binary remains byte-for-byte identical to v1.0.1: the `BetaConfig.isProxyDaemonEnabled(ctx)` check short-circuits before any binder touch. **(9) Build.** `versionCode` 176 → 177, `versionName` pinned at `"1.1.9"` per channel policy. No new strings, layouts, drawables, or dependencies. **(10) What's left.** Phase 4 is now feature-complete for every production hot path: `wm overscan` (4a) → typed; `pidof` (4b) → typed; `service call AutoContainer 2 …` (4c) → typed. Remaining shell uses are intentionally diagnostic/self-test (`SysInfoActivity` info dump, `Diag → ADB` button, `BetaTestRunner` X-family cross-checks). Probes P3/P4 (`forceStopPackage` / `killBackgroundProcesses`) remain available if a future need surfaces — Phase 4d would be a one-off then. |
| **1.1.9-build176** | 176 | **Beta Engine — Phase 4c feasibility probe P13 (`AutoContainer.transact(2)` direct).** Diagnostic-only build. Zero production code change vs build 175 — `ShellGateway`, `MainActivity`, `ClusterService` behaviour identical. Sole purpose: confirm whether the daemon (uid 2000, SELinux `shell`) can call `binder.transact(code=2, ...)` directly on the BYD `AutoContainer` system service, bypassing the `service call AutoContainer 2 i32 …` shell wrapper used by `AdbLocalClient.sendInfo` (the busiest remaining shell call in the projection start/stop critical path). **(1) Why a new probe.** Build-173 P6 only confirmed the binder is reachable and exposed descriptor `android.os.IAutoContainer` via `INTERFACE_TRANSACTION` — a benign metadata query that doesn't exercise the per-method permission/uid check. P13 actually invokes transaction code 2 with the exact wire format used in production (`writeInterfaceToken(descr)` + `writeInt(type)` + `writeInt(info)` + `writeString(str)`) and a known idempotent payload (`type=1000, info=30, str=""` — the "Seal EU screen size" notification already triggered by the Diag → ADB diagnostic dump button, so even on PASS no visible side effect beyond what the user can already trigger themselves). If P13 PASSES, Phase 4c becomes trivial (one typed verb, one regex in `ShellGateway`, ~80 LoC). If P13 FAILs with `FAIL_SECURITY`, the OEM has a uid/signature check on the actual transaction that the shell wrapper somehow satisfies (likely via the `service` binary running with a different context or by going through a permission-broker hop) and Phase 4c needs a different approach. **(2) Implementation.** New `Phase4Probes.p13_autoContainerDirectSendInfo()` (~35 LoC). First re-queries the descriptor via `INTERFACE_TRANSACTION` (defensive — if the OEM ever rebrands the service, we fail fast with `FAIL_OTHER:no descriptor` instead of silently writing the wrong interface token). Then builds the parcel exactly as `service call` would (`writeInterfaceToken` uses the live descriptor so we're robust against OEM rebrands), calls `transact(2, ...)` non-oneway, and `readException()` so any remote `SecurityException` / `IllegalArgumentException` surfaces in the structured failure taxonomy (`FAIL_SECURITY` / `FAIL_OTHER`). The probe is the 13th and last entry in `Phase4Probes.runAll`. **(3) `BetaTestRunner` — Family `P` catalog grows from 12 to 13 entries.** Added `TestDef("P13", Family.P, "AutoContainer.transact(2) direct", …)` after P12. The `runOne` switch grew a single new case label `case "P13":` — the dispatch hits the existing shared `testProbe(ctx, r)` helper that parses the daemon-returned `STATUS:detail` token, so no new code path is needed. The shared `sProbeCache` (introduced in build 173) still amortises one daemon round-trip across the entire P-family per suite run. **(4) Wire / protocol.** No new transaction codes, no protocol version bump. The probe call uses the existing `TXN_PROBE_PHASE4` (=4) channel established in build 173 — the daemon-side `Phase4Probes.runAll` simply runs one more probe and appends the 13th token to the pipe-separated result string. Client-side `Phase4Probes.parse` is a generic Map builder and handles any number of tokens. **(5) Expected outcomes.** Most likely: P13 PASSES (the descriptor probe in P6 already worked, the AOSP `AutoContainer` design typically only checks uid range for who-can-set-screen-size — exactly what shell uid 2000 satisfies). Second-most-likely: `FAIL_SECURITY: SecurityException requires signature` if the OEM added a `mContext.enforceCallingPermission(…)` to the service implementation. In that case the next experiment will be to inspect what context the `service` binary runs under (it may be doing a per-method `clearCallingIdentity` trick we can replicate from the daemon). Either result is a deterministic decision: PASS → ship Phase 4c in build 177, FAIL_SECURITY → escalate to a write-up of the AutoContainer fork-and-jump pattern needed. **(6) Diag UI surfacing.** DiagActivity → Beta Engine tab now lists 28 rows (was 27). The new P13 row sits after P12 in the table; the row title shows the human-readable verb (`AutoContainer.transact(2) direct`) and on a result the cell turns green (`PASS`) / red (`FAIL_SECURITY` / `FAIL_OTHER`) with the unwrapped reason in the message column. **(7) Behavioural impact for end users.** None. The new transaction is only triggered when the user opens Diag → Beta Engine → Run All. With the beta proxy toggle OFF, the entire P family is `FAIL: daemon probe run failed: daemon not connected`, which is the expected legacy-only behaviour. The shipping binary remains byte-for-byte identical to v1.0.1 with both beta switches OFF. **(8) Build.** `versionCode` 175 → 176, `versionName` pinned at `"1.1.9"` per channel policy. No new strings, layouts, drawables, or dependencies — purely a one-method addition in `Phase4Probes` plus a one-line entry in `BetaTestRunner.catalog()` and one `case` label. |
| **1.1.9-build175** | 175 | **Beta Engine Phase 4b — typed `pidof` replaces the `sh -c "pidof <pkg>"` fork.** Second production cash-in of the build-173 probe matrix, building directly on the build-174 Phase 4a infrastructure. Validated against the build-174 device log (`byd_log_20260521_231046.log`): Phase 4a measured 8 typed `setOverscan` calls at 11–61 ms (warm) with **zero** shell-fallback log lines, but the same log still showed 7 `beta runShell ok (Xms): pidof com.waze` lines at **48–181 ms** every ~5 s — the next obvious hot path. **(1) `Phase4Verbs.getPidsByPackage(String)`** — new pure-Java verb that opens `/proc`, iterates the numeric PID directories, reads up to 256 bytes of each `/proc/<pid>/cmdline`, splits on the first NUL to extract `argv[0]`, and collects every PID whose argv[0] equals `packageName` or starts with `"packageName:"` (Android sub-processes declared via `android:process=":foo"` in the manifest, exactly what shell `pidof` matches). Returns a space-separated list of PIDs (same wire shape as `pidof` stdout), empty string when nothing matches. Skips non-numeric `/proc` entries cheaply with a manual digit loop instead of `Integer.parseInt`/try-catch, and pre-checks `canRead()` so unreadable kernel threads don't trip `IOException`. Probe P8 (build 173) measured the same scan at **0 ms** on the BYD Seal EU with 241 live processes — replacing a 48–181 ms fork with a ~0–5 ms in-process scan. The choice of `/proc` scan over `IActivityManager.getRunningAppProcesses()` is deliberate: `/proc` is immune to the Android-10 `getRunningAppProcesses` visibility restrictions (which can return an empty list to non-system callers on some OEM builds), it doesn't need any system Context handle, and it precisely reproduces shell `pidof`'s argv[0] semantics. **(2) `ProxyDaemonMain.TXN_GET_PIDS = FIRST_CALL_TRANSACTION + 5` (=6).** Wire format: `String packageName` in → `String spaceSeparatedPids` out (or `writeException` on the unlikely scenario that the scan throws). Defensive try/catch around the scan call: any `Throwable` is unwrapped to a real `Exception` and surfaced via `writeException` so the client falls back to shell with full diagnostic context. Protocol version stays at `"2"` — purely additive transaction. **(3) `BetaProxyClient.getPidsByPackage(String) throws BetaProxyException`** — mirrors the established `runShell` / `setOverscan` Parcel pattern (`writeInterfaceToken` + `writeString` + `transact(TXN_GET_PIDS)` + `readException` + `readString`). Daemon-side exceptions surface as `BetaProxyException` so `ShellGateway` can transparently fall back to the shell path. `RemoteException` nulls the cached binder so the next call triggers a fresh connect. **(4) `ShellGateway` — typed-verb dispatch refactor.** The previous `boolean tryTypedVerb(...)` is now `String tryTypedVerb(...)` returning the stdout payload to hand to the caller's callback on success, or `null` when the command didn't match or the typed call failed. This was needed because `pidof` produces a real payload (the PID list) that the callers consume via `onSuccess(output).trim().isEmpty()` — overscan callers were unaffected because they always received `""` and continue to do so. New pattern `PIDOF = ^\\s*pidof\\s+([A-Za-z0-9._:\\-]+)\\s*$` anchored on both ends and restricted to a single argument so multi-word `pidof a b c` (and any compound invocations) fall through to shell. On match: `BetaProxyClient.getPidsByPackage(pkg)` → log `beta pidof typed ok (Xms): <pkg> → "<pids>"` → callback receives the same string the shell would have produced. **(5) Sites that benefit automatically.** Both `pidof` call sites in `MainActivity` (`reconcileDisplayState` for the cluster app, `reconcileMainDisplayState` for the main-display marker) are unchanged — they already route through `ShellGateway`. They fire every ~5 s while a cluster app or main-display app is alive (driven by `mDisplayPollHandler`). On a build-174 30-minute session, that's ~720 forks/min × 30 min × 2 sites = up to ~720 forks eliminated per session, each ~50–180 ms = **CPU and battery cycles measurable in the dashcam recording timeline**. **(6) Fallback chain still rock-solid.** typed → shell-via-daemon → legacy ADB. If `/proc` scan ever returns an unexpected empty result on a future OEM build that puts apps in cgroups invisible to uid 2000, the typed call succeeds with `""`, callers interpret that as "process dead" (legitimate semantics), and worst case the cluster bookkeeping is cleared one cycle early. If the daemon transaction itself fails (e.g. daemon dead), `BetaProxyException` triggers the existing fall-through to `runShell` → legacy. **(7) Behavioural contract preserved.** No public API removed, no source change in `MainActivity` / `ClusterService` since build 174. With the beta proxy toggle OFF the binary remains byte-for-byte identical to v1.0.1 (gateway short-circuits to `AdbLocalClient` before any pattern matching). **(8) Build.** `versionCode` 174 → 175, `versionName` pinned at `"1.1.9"` per channel policy. No new strings, layouts, drawables, dependencies. **(9) What's left for Phase 4.** Phase 4c (probe P6 confirmed `IAutoContainer` descriptor) — direct typed transact for `AdbLocalClient.sendInfo` to bypass the BYD shell wrapper. Phase 4d (optional, probes P3/P4 to be re-confirmed against shell uid) — typed `forceStopPackage` / `killBackgroundProcesses`. |
| **1.1.9-build174** | 174 | **Beta Engine Phase 4a — typed `IWindowManager.setOverscan` replaces the `wm overscan` shell fork.** First production cash-in of the build-173 probe results (P1–P12 all PASS on BYD Seal EU). Every `wm overscan L,T,R,B -d N` and `wm overscan reset -d N` command produced by `ShellGateway` is now intercepted client-side, parsed, and routed through a typed Binder call to `IWindowManager.setOverscan(displayId, l, t, r, b)` inside the daemon — bypassing the `ProcessBuilder("sh", "-c", "wm overscan …")` JVM fork that took 70–275 ms per call in build 172 (`ClusterService:525/595`, `MainActivity.autoApplyInsetsIfNeeded`, resize SeekBar). **(1) New class `com.byd.dashcast.beta.proxy.Phase4Verbs`** (~70 LoC). Lives in the daemon process. Holds a lazy double-checked cache of the `IWindowManager` proxy and the `setOverscan(int,int,int,int,int)` `Method` so subsequent calls skip `Class.forName` / `ServiceManager.getService` / `asInterface` entirely — critical for the resize SeekBar which fires 30+ overscan changes per second while the user drags. `setOverscan(displayId, l, t, r, b) throws Throwable` is the single public verb in this build. **(2) `ProxyDaemonMain` — new `TXN_SET_OVERSCAN` (=5) transaction.** Wire format: `int displayId, int l, int t, int r, int b` in → empty reply (or `writeException` on failure). The onTransact case unwraps `InvocationTargetException` so the client receives the *real* underlying exception (typically a `SecurityException` or `IllegalArgumentException` from the framework) rather than a generic reflection wrapper. Protocol version stays at `"2"` — purely additive. **(3) `BetaProxyClient.setOverscan(int displayId, int l, int t, int r, int b) throws BetaProxyException`** — new public method mirroring the established `runShell(String)` Parcel pattern (`writeInterfaceToken` / 5× `writeInt` / `transact(TXN_SET_OVERSCAN)` / `readException`). Daemon-side exceptions surface as `BetaProxyException` so `ShellGateway` can transparently fall back. **(4) `ShellGateway` — pattern-matching typed-verb interception.** Two pre-compiled regex patterns (`WM_OVERSCAN = ^wm overscan (-?\\d+),(-?\\d+),(-?\\d+),(-?\\d+) -d (\\d+)$` and `WM_OVERSCAN_RESET = ^wm overscan reset -d (\\d+)$`) test the command before any shell call. On match: `BetaProxyClient.setOverscan(...)` is invoked; on success the original callback receives `onSuccess("")` (overscan commands produce no useful stdout) and we log `beta setOverscan typed ok (Xms): d=N L,T,R,B`. On regex miss OR typed-call exception, the code falls through to the existing `runShell` path (which itself falls back to legacy `AdbLocalClient` on its own failure). The fallback chain is now **typed → shell-via-daemon → legacy ADB** instead of the previous **shell-via-daemon → legacy ADB**, so a freshly-installed-but-OEM-modified device that rejects the typed call still ends up doing exactly what build 172 did. Anchored regex (`^…$`) on both ends so any compound or unusual command (e.g. `wm overscan … && wm size …`) cleanly falls through. **(5) Sites that benefit automatically.** All 7 production call sites migrated in build 172 already route through `ShellGateway`, so the speedup is transparent: `MainActivity` resize SeekBar (~30 calls/s while dragging), `autoApplyInsetsIfNeeded` (500 ms post-launch per-app overscan), `ClusterService.startActivityViaIAM` (launch-time overscan), `ClusterService.stopProjectionNoAdb` (`wm overscan reset -d 1`), `ClusterService` post-projection apply (`INSET_H,INSET_V,INSET_H,INSET_V`). The two `pidof` sites in `reconcileDisplayState` / `reconcileMainDisplayState` are **not** affected by Phase 4a (no `wm overscan` match) — they keep going through `runShell` and will be migrated in Phase 4b via `IActivityManager.getRunningAppProcesses`/`getTasks` (probe P5 PASS in build 173). **(6) Behavioural contract preserved.** No public API removed. `MainActivity` / `ClusterService` source unchanged from build 173. With the beta proxy toggle OFF the binary remains byte-for-byte identical to v1.0.1. Order of overscan calls is still preserved (the single-threaded `shell-gateway` executor stays as the serialisation point). **(7) Expected device behaviour.** The build-172 log showed `ClusterService L595` at 1687 ms (cold), `L525` at 275 ms, `MainActivity autoApplyInsets` at 84 ms, resize SeekBar at 197 ms. With Phase 4a we expect those to drop to single-digit ms after the first call warms `Phase4Verbs`' reflection cache (probe P1 measured 117 ms on the very first call due to the same cache warm-up, then 0 ms on subsequent runs). The SeekBar drag in particular should feel instantaneous. **(8) Build.** `versionCode` 173 → 174, `versionName` pinned at `"1.1.9"` per channel policy. No new strings, layouts, drawables, dependencies. |
| **1.1.9-build173** | 173 | **Beta Engine — Phase 4 feasibility probe suite (P1–P12).** Diagnostic-only build whose sole purpose is to answer one question before we commit to the Phase 4 rewrite: *"which native AOSP APIs can the proxy daemon actually call from uid 2000 / SELinux domain `shell` on the BYD Seal EU?"* No production code path changes — `ShellGateway`, `MainActivity`, `ClusterService` behaviour is identical to build 172. **(1) New class `com.byd.dashcast.beta.proxy.Phase4Probes`** (~330 LoC). Runs **inside the daemon process** and tries every typed Binder verb that Phase 4 would replace a `sh -c "wm …"` / `am …` fork with. Twelve probes total: **P1** `IWindowManager.setOverscan(0,0,0,0,0)` — the headline verb behind every `wm overscan` shell call in the resize SeekBar and `autoApplyInsetsIfNeeded`; **P2** `IWindowManager.getInitialDisplaySize(0, Point)` — read-only counterpart, useful to bypass `wm size`/`dumpsys display`; **P3** `IActivityManager.forceStopPackage("com.example.does.not.exist", -1)` — replaces `am force-stop`; **P4** `IActivityManager.killBackgroundProcesses("com.example.does.not.exist", -1)` — replaces `am kill`; **P5** `IActivityManager.getTasks(20)` (with `getRunningTasks(int)` fallback) — replaces the `dumpsys activity recents` + regex pipeline used by `ClusterService.findRunningTaskId`; **P6** raw `INTERFACE_TRANSACTION` against the `AutoContainer` system service — the missing piece to migrate `AdbLocalClient.sendInfo` away from the BYD shell wrapper; **P7** `SystemProperties.get(ro.build.version.release)` + `SystemProperties.set(debug.dashcast.probe, "1")` — read should PASS, write almost certainly fails (sepolicy), detail string carries the exact exception class; **P8** pure-Java scan of `/proc/*/cmdline` for `com.byd.dashcast` — sandbox baseline, should always PASS; if it doesn't, the daemon is sandboxed harder than expected and the entire Phase 4 plan is doomed regardless of any Binder permission; **P9** `ServiceManager.listServices()` enumeration with explicit visibility check for `window` / `activity` / `package` / `input` / `display` / `AutoContainer` / `media_session` / `audio`; **P10** `IPackageManager.getInstallerPackageName("com.byd.dashcast")` — typed PM read; **P11** `IInputManager.Stub.asInterface` + walk of `IInputManager.getDeclaredMethods()` to confirm `injectInputEvent` is reachable (already known to work via the input forwarder — sanity check); **P12** `DisplayManager.getDisplays()` via the daemon's system Context — confirms the framework managers attached to the system Context (already proven by `BetaSystemContext`) are usable from inside the daemon process too. Each probe is wrapped in a single try/catch that **unwraps `InvocationTargetException`** and categorises the failure into one of four buckets: `PASS`, `FAIL_SECURITY` (SecurityException → Phase 4 verb NOT viable from uid 2000), `FAIL_API` (`NoSuchMethodException` / `ClassNotFoundException` → API renamed on this OEM, need device-specific reflection), `FAIL_OTHER` (any other Throwable, detail carries `ClassName message`). Results are concatenated as `P1=PASS:detail|P2=FAIL_SECURITY:detail|…` with `|` and newlines stripped from detail strings to keep the parser trivial. The harness itself never crashes the daemon — any exception escaping `runAll` is caught in the binder layer and returned as `P0=FAIL_OTHER:harness …`. **(2) `ProxyDaemonMain` — new `TXN_PROBE_PHASE4` (=4) transaction.** Wire format: no args in → `String` out. The system Context (already acquired by `acquireSystemContext()` for the connect broadcast) is now also stored in `private static volatile Context sSystemContext` so `ProxyBinder.onTransact` can pass it to `Phase4Probes.runAll(...)` without redoing the reflection. Protocol version stays at `"2"` because the new transaction is purely additive — older clients keep working unchanged. **(3) `BetaProxyClient.runPhase4Probes()` — new public client method**, mirrors the `runShell(String)` pattern (`writeInterfaceToken` / `transact(TXN_PROBE_PHASE4)` / `readException` / `readString`) and rethrows as `BetaProxyException` on transact failure. **(4) `BetaTestRunner` — Family `P` and 12 new tests P1–P12.** `Family` enum extended with `P` (next to `A`, `B`, `X`). Catalog grows from 15 to 27 entries with the 12 probe rows appended after `X3`. `runOne()` switch grew a single shared case-block that dispatches every P-id to a new `testProbe(ctx, r)` helper. To avoid running the probe suite 12 times (once per row in the diag UI), the first P-test populates two static fields: `sProbeCache` (parsed `Map<String,String>` of probe-id → `"STATUS:detail"`) and `sProbeError` (set if the daemon connect or the transact itself failed). Subsequent P-tests hit the cache. The status mapping is strict: only `STATUS == "PASS"` reports green; everything else (including `FAIL_SECURITY`, `FAIL_API`, `FAIL_NULL`, `FAIL_OTHER`) is rendered as a red FAIL with the category prepended to the message so the row title in `DiagActivity` reads e.g. `FAIL_SECURITY: SecurityException Neither user 2000 nor current process has android.permission.WRITE_SECURE_SETTINGS`. The cache is reset at the start of each `runAll()` invocation so re-running the suite re-queries the daemon. **(5) Behavioural impact.** Production users see nothing — neither `ShellGateway` nor the migrated `MainActivity` / `ClusterService` call sites changed at all, and the new transaction is only triggered if the user opens **Diag → Beta Engine** and runs the suite. With the beta toggle OFF the entire P family is `FAIL` with message `daemon probe run failed: daemon not connected`, which is the expected legacy-only behaviour. **(6) What we learn from a single device run.** Each `PASS` row maps 1:1 to a Phase 4 verb that is safe to ship; each `FAIL_SECURITY` row tells us exactly which verb has to stay on the shell path (or be wrapped behind a different mechanism, e.g. a system signature shim); the `FAIL_API` taxonomy isolates OEM divergences from outright denials. The expected steady-state outcome is roughly: P8 always PASS, P2/P5/P9/P10/P11/P12 PASS, P1/P3/P4 PASS (most likely on shell uid for AOSP — to be confirmed), P6 PASS for INTERFACE_TRANSACTION only (the actual sendInfo verb still has to be triaged separately), P7 read=PASS write=FAIL. **(7) Build.** `versionCode` 172 → 173, `versionName` pinned at `"1.1.9"` per channel policy. No new strings, layouts or drawables. No public API removed — `Phase4Probes.runAll(Context)` and `BetaProxyClient.runPhase4Probes()` are purely additive. |
| **1.1.9-build172** | 172 | **Beta Engine Phase 3 — production call-site migration to the proxy daemon.** Build 171 made the proxy daemon production-quality (12 ✓ / 0 ✗ / 3 ⊘ on device, X1 measured at 0 ms beta vs 68 ms legacy — a 60× shell-call latency win). This build cashes that win in: the seven hot-path callers of `AdbLocalClient.executeShell` / `executeShellWithResult` in `MainActivity` and `ClusterService` now route through the proxy when `beta_proxy_enabled` is ON, while staying byte-for-byte identical to v1.0.1 when the toggle is OFF. **(1) New class `com.byd.dashcast.beta.ShellGateway`** (~80 LoC). Drop-in wrapper with the same signatures as the legacy methods (`execShell(ctx, cmd)` and `execShellWithResult(ctx, cmd, AdbLocalClient.Callback)`). Owns its own dedicated single-threaded executor (`shell-gateway`, daemon thread) so call order is preserved within a process. Fast path: if `BetaConfig.isProxyDaemonEnabled(ctx)` returns `false`, delegates synchronously to `AdbLocalClient` with zero overhead. Beta path: lazily connects to the daemon via `BetaProxyClient.connect(ctx)` if needed, then calls `BetaProxyClient.runShell(cmd)` and logs `beta runShell ok (Xms): <cmd>`. On any `Throwable` (RemoteException, daemon dead, timeout, etc.) it logs a warning and transparently falls back to `AdbLocalClient.executeShellWithResult(ctx, cmd, cb)` — the original callback is honoured by the fallback path so consumers never see the swap. **(2) `MainActivity` (4 sites migrated).** `AdbLocalClient.executeShell` → `ShellGateway.execShell` in the resize-SeekBar listener (`wm overscan w,h,w,h -d 1`) and in `autoApplyInsetsIfNeeded` (per-app saved overscan, fires 500 ms after launch). `AdbLocalClient.executeShellWithResult` → `ShellGateway.execShellWithResult` in `reconcileDisplayState` (`pidof <clusterPkg>` polling, runs every ~5 s while a cluster app is alive) and `reconcileMainDisplayState` (`pidof <mainPkg>` for the main-display marker). These four are the ones that fire most often in normal usage. **(3) `ClusterService` (3 sites migrated).** All three `wm overscan` system calls — the launch-time per-app overscan in `startActivityViaIAM` setup, the `stopProjectionNoAdb` reset (`wm overscan reset -d 1`), and the post-projection-active overscan apply with `INSET_H,INSET_V,INSET_H,INSET_V` on the cluster display. **(4) Sites intentionally NOT migrated.** `SysInfoActivity`, `SettingsActivity` (`Test ADB` button), `DiLink5TestRunner` and `BetaTestRunner` keep calling `AdbLocalClient.executeShellWithResult` directly because they are diagnostic / self-test surfaces that must exercise the legacy path on purpose (otherwise the test runners would compare beta against beta). `BetaProxyClient` itself keeps using `AdbLocalClient` for its own bootstrap shell (`setsid sh -c …`) and for `readDaemonLogTail` — the proxy obviously cannot route itself. **(5) Build.** `versionCode` 171 → 172, `versionName` pinned at `"1.1.9"` per the channel policy (`1.2.0` reserved for DiLink 5). **Expected device behaviour with the toggle ON**: `wm overscan` and `pidof` calls drop from ~70 ms each to ~0 ms (single-digit ms) as measured by X1 in build 171 — most visible on the resize SeekBar (no more visible lag when dragging), on the per-app overscan auto-apply 500 ms after launch (felt as instantaneous), and on the pidof-based reconciliation timer (lower CPU per poll). **With the toggle OFF** the binary is functionally identical to v1.1.9 build 171 — every migrated site short-circuits to the same `AdbLocalClient` method it was calling before, no executor overhead, no proxy connection attempt. **Regression / fallback safety.** Any failure on the beta path (daemon down, transact failure, exception in `runShell`, etc.) is caught and logged as `ShellGateway: beta runShell failed after Xms, fallback legacy: <msg> [cmd=<cmd>]`, then the same callback is invoked through the legacy path with the same arguments. The user never sees a broken cluster operation because of a proxy hiccup. The dedicated `shell-gateway` executor uses a single thread so beta-then-legacy fallback for the same call cannot interleave with other in-flight commands. **Public API surface preserved.** `BetaProxyClient` and `AdbLocalClient` signatures unchanged. `ShellGateway` is purely additive. No layout / strings / drawable changes in this build. |
| **1.1.0-beta1** | 156 | **Beta Engine scaffolding + Diagnostics rebuild + file-based log sharing (full i18n).** First pre-release on the new `beta/1.1.0` channel. Introduces an opt-in "Beta Engine" pipeline that is wired everywhere needed but **stays inert at runtime by default** so the v1.0.1 stable code path remains the source of truth. **(1) New `com.byd.dashcast.beta` package** (5 classes, ~654 LoC). `BetaConfig` (67 LoC) — SharedPreferences-backed (`byd_app_prefs`, same store as `SettingsActivity`) feature flags: `beta_engine_enabled` and `beta_proxy_enabled` (both default `false`). Granularity = option B (two independent toggles). Activation semantics = next launch (toggles are read once at process start by `BetaSystemContext.refresh()`, and a restart-required dialog is shown when the user flips them). `BetaSystemContext` (119 LoC) — process-wide singleton snapshot of the beta state, exposes `isEngineEnabled()` / `isProxyEnabled()` to the rest of the app without re-reading prefs in hot paths. `BetaProxyClient` (94 LoC) — stub client that will eventually route ADB calls through an alternate transport; currently returns `NOT_IMPLEMENTED` for every probe. `BetaEngineGateway` (65 LoC) — single entry point used by future call-sites, exposes a `safeCall(legacy, beta)` helper that **auto-falls back to the legacy implementation on any exception** so the user can keep using the app even if the beta path fails. `BetaTestRunner` (309 LoC) — 15-test self-check matrix (B1–B6 real beta-engine probes, A1–A6 currently `SKIPPED` placeholders for the ADB-bridge work, X1–X3 mixed cross-checks), each test returns a `Result` with status (`PASS` / `FAIL` / `SKIPPED` / `WARN`), latency in ms and a short message. **(2) `SettingsActivity` (+36 LoC).** New "Beta (expérimental)" card containing two `MaterialSwitch` rows (`sw_beta_engine`, `sw_beta_proxy`), an amber warning banner (`bg_beta_warning` drawable, `bg_beta_pill` for the "BETA" pill) explicitly stating the beta nature, and a restart-required `AlertDialog` triggered whenever either switch is toggled. The card is **always visible** (no hidden gesture, no developer mode required) because the v1.1.0 line is a beta channel and the user opted-in by installing the beta APK. Toggle changes persist through `BetaConfig.set*()` but have **no runtime ADB effect yet** — flipping them off restores the exact v1.0.1 behaviour, flipping them on currently only enables the diagnostics readouts and the auto-fallback path in `BetaEngineGateway`. **(3) `DiagActivity` rebuilt as a 6-tab `TabLayout`** (~257 LoC). Tabs in order: **Beta Engine** (default selected, runs the 15-test `BetaTestRunner` and renders a colour-coded card list via `item_beta_test.xml` + `include_diag_beta_engine.xml`, with a "Lancer tous" / "Run all" button and a "Envoyer (.log)" / "Send (.log)" button), Système, Cluster, ADB, Logs, Apps. The five legacy tabs share a single `include_diag_coming_soon.xml` placeholder that prints "Bientôt disponible" — the existing 1.0.1 diag screens are kept reachable via the overflow menu and will be migrated tab by tab in subsequent beta drops. **(4) `AppLogger` refactor (file-based sharing everywhere, +file API).** Added `writeFile(ctx, prefix, content)` — generic timestamped `.log` writer under `getExternalFilesDir("logs")` with a `files-path` fallback; added `shareFile(ctx, File, subject, chooserTitle)` — generic `Intent.ACTION_SEND` with `EXTRA_STREAM` via the existing FileProvider authority (`${packageName}.fileprovider`, `xml/file_paths.xml` covers both `external-files-path` and `files-path`) and `FLAG_GRANT_READ_URI_PERMISSION`. The existing `share(ctx)` is now file-first (attaches a `byd_log_<timestamp>.log`) with a plain-text fallback only when the file write fails. New `shareWithReport(ctx, reportText)` writes a combined `byd_report_<ts>.log` (header line + diagnostic report + full log buffer) and attaches it via `shareFile`. Net effect: **every "Send logs" / "Send report" action in the app now ships a real `.log` file** instead of a raw text body, which fixes the previous 64 KB-ish silent truncation seen on Android share-sheets and lets the user attach the file to GitHub issues / e-mail / WhatsApp without copy-paste loss. **(5) New layouts and drawables.** `layout/include_diag_beta_engine.xml`, `layout/include_diag_coming_soon.xml`, `layout/item_beta_test.xml`, `drawable/bg_beta_pill.xml`, `drawable/bg_beta_warning.xml`, and edits to `layout/activity_diag.xml` (TabLayout root) and `layout/activity_settings.xml` (Beta card insertion). **(6) Strings — full i18n.** Added a block of 28 new keys ending with `diag_beta_run_all` / `diag_beta_copy_report` / `diag_beta_report_copied` to `values/strings.xml` (FR default) and `values-en/strings.xml`. Translated all 28 keys to the 10 remaining locales (`ar`, `be`, `de`, `es`, `it`, `kk`, `ru`, `tr`, `uk`, `uz`) via `scripts/inject_beta_strings.py` (idempotent — re-runs are no-ops thanks to a `diag_beta_run_all` marker check). 12 locales total, parity with the existing UI. **(7) Build.** `versionCode` 155 → 156, `versionName` `"1.0.1"` → `"1.1.0-beta1"`. No business logic from v1.0.1 is modified — cluster projection, overscan, OTA, restore-origin-cluster, app shortcuts, category filters, reconnect popup and visual mode all behave exactly as in v1.0.1 with both beta switches OFF. **Known limitations of this drop**: A1–A6 tests show `SKIPPED`; the proxy ADB transport is a stub; the legacy 5 diag tabs render a placeholder. These are intentional and will be filled in subsequent `1.1.0-betaN` drops on the same channel. |
| **0.9.61-alpha** | 127 | **Settings icons — drop the style attribute entirely.** Even with `android:layout_width="28dp"`/`layout_height="28dp"` inlined on the View tag in 0.9.6, the leading icons still rendered as tiny white dots on the BYD device. Compared to the `+`/`−` slider icons (which render correctly), the only remaining difference was that the leading `<ImageView>` still had `style="@style/Widget.DashCast.ListItem.Leading"` applied. Removed the `style="…"` attribute from the 7 leading `<ImageView>` instances in `activity_settings.xml` and inlined every needed attribute directly on the View tag (`layout_width=28dp`, `layout_height=28dp`, `layout_marginEnd=16dp`, `layout_gravity=center_vertical`, `scaleType=fitCenter`, `tint=@color/md_on_surface_variant`, plus the existing `src`). No other change. |
| **0.9.6-alpha** | 126 | **Settings icons — the actual root cause.** Reported on the car after 0.9.5: the icons in `Comportement` and `À propos` were rendering as tiny white dots, regardless of the `android:tint` value. Root cause finally found: on this BYD Android 10 runtime, `android:layout_width` and `android:layout_height` declared inside a `<style>` (here `Widget.DashCast.ListItem.Leading` with `28dp × 28dp`) are not honored by `LayoutInflater` because layout params are extracted from the View tag itself, not from the applied style. The ImageView therefore fell back to a near-zero size, so the vector was rendered as a 1–2 px white dot. Fix: inlined `android:layout_width="28dp"`, `android:layout_height="28dp"`, `android:layout_marginEnd="16dp"` on every leading `<ImageView>` in `activity_settings.xml` (7 occurrences). The style still applies `layout_gravity`, `scaleType` and other non-layout attributes. No other change. |
| **0.9.5-alpha** | 125 | **Settings icons — third and final fix attempt.** In 0.9.3 the tint was on the vector drawable (didn't work). In 0.9.4 it was inlined as `android:tint="?attr/colorOnSurfaceVariant"` on each `<ImageView>` (still didn't work on the BYD device — the theme attribute reference returned a transparent value at runtime, so every leading icon in Comportement, À propos and the +/- slider icons rendered as empty space). Final fix: replaced every `android:tint="?attr/colorOnSurfaceVariant"` with `android:tint="@color/md_on_surface_variant"` (8 occurrences in `activity_settings.xml`) and `android:tint="?attr/colorOnPrimaryContainer"` with `android:tint="@color/md_on_primary_container"` on the Welcome hero `ic_cast`. The color resource is defined in both `values/colors.xml` (`#43474E`) and `values-night/colors.xml` (`#C3C6CF`), so DayNight is still respected through resource-qualifier resolution instead of theme-attribute indirection — which is what actually works on this Android 10 BYD runtime. No other change. |
| **0.9.4-alpha** | 124 | **Mockup fidelity & OTA parity fixes on top of 0.9.3.** **Settings — leading icons now visible (real fix)**: in 0.9.3 the `?attr/colorOnSurfaceVariant` tint was moved from `<vector>` drawables to the `Widget.DashCast.ListItem.Leading` style, but on the BYD device the runtime never propagated that theme-attribute tint through the style either, so the row icons in `Comportement` and `À propos` were still invisible. Root-cause fix: removed `android:tint="?attr/colorOnSurfaceVariant"` from the 8 affected vector drawables (`ic_add`, `ic_autorenew`, `ic_filter_list`, `ic_info_outline`, `ic_remove`, `ic_restart_alt`, `ic_save`, `ic_system_update`) and from `ic_cast.xml` (`?attr/colorOnPrimaryContainer`), removed the tint item from the `Leading` style, and inlined `android:tint="?attr/colorOnSurfaceVariant"` directly on every leading `<ImageView>` in `activity_settings.xml` (7 occurrences across the cluster type, overscan, behavior and about cards, plus the science pre-release row). The hero `ic_cast` in `activity_welcome.xml` got an inline `android:tint="?attr/colorOnPrimaryContainer"`. **Settings — `Vérifier` button now triggers the real OTA flow**: in 0.9.3 the `btn_check_update` opened the GitHub Releases page in a browser, which did not match the 3-dot-menu OTA. Refactored `MainActivity.makeOtaProgressListener(boolean)` (instance method, ~125 lines) into `public static makeOtaProgressListener(Activity, boolean)` by replacing every `MainActivity.this` / `getString` / `getColor` / `getResources()` / `isFinishing()` / `isDestroyed()` with the passed `activity` reference. Both existing internal callsites (`MainActivity.onCreate` auto-check at launch and `showOverflowMenu` case 6) were updated to pass the activity. `SettingsActivity.btn_check_update` now calls `UpdateChecker.checkUpdate(this, MainActivity.makeOtaProgressListener(this, true))` — same `AlertDialog` with `ProgressBar` for download, same `startDownload` / `onInstalling` / `onUpToDate` / `onError` handlers as the 3-dot menu. `row_source_code` still opens `https://github.com/Kiroha/byd-dashcast` via `openUrl()`. The supporting text below `Rechercher une mise à jour` was rewritten from `Ouvre la page des releases sur GitHub` to `Vérifie si une nouvelle version est disponible`. **Welcome card — full-width + Continue button**: the card was capped at 800dp in 0.9.3 and still felt narrow; it now uses `match_parent` with 60dp horizontal padding so it spans the full screen, the hero square shrinks from 96dp to 80dp, the title from 44sp to 36sp, the language-select prompt from 16sp to 15sp, vertical paddings/margins are tightened so the four rows of three language buttons fit without scrolling on the 12.3" cluster. Added a new `Widget.DashCast.Button.Text` button `btn_continue_without_change` with the `ic_arrow_forward` trailing icon: tapping it calls `LocaleHelper.markSetupDone(this)` and starts `MainActivity` without changing the locale (matches the mockup's `welcome_continue_without_change` action). **Settings — full i18n pass**: extracted 13 remaining hardcoded French strings into `values/strings.xml` (`settings_section_behavior`, `settings_section_about`, `settings_cluster_88_desc`, `settings_cluster_123_desc`, `settings_cluster_1025_desc`, `settings_boot_auto_start_support`, `settings_reconnect_popup_support`, `settings_category_filters_support`, `settings_about_version_label`, `settings_check_update_title`, `settings_check_update_support`, `settings_check_update_button`, `settings_source_code_title`) and translated them into the 12 supported locales (fr, en, de, es, it, tr, ru, uk, ar, uz, kk, be). The `welcome_continue_without_change` key was also added for the new button in the same 12 locales. No business logic changed: cluster type, overscan, behavior toggles, pre-release channel, visual mode and OTA install still save and load exactly as before. |
| **0.9.3-alpha** | 123 | **Mockup fidelity fixes on Welcome & Settings (follow-up to 0.9.2)**. **Welcome**: the language-picker card was shrinking to the intrinsic width of the button grid because the outer `MaterialCardView` was `wrap_content`; it now uses an explicit `800dp` width (matching the mockup's `max-width: 800px`), and the inner `LinearLayout` is `match_parent`, so the 12 outlined language buttons (`weight=1`) actually fill the card from edge to edge as in `mockups/mockup_m3.html` screen #1. **Settings — leading icons now visible**: the per-row icons in the `Comportement` and `À propos` cards were defined in XML but invisible at runtime because the `?attr/colorOnSurfaceVariant` tint was declared inside each `<vector>` drawable, where theme attribute resolution is unreliable across Android versions. The tint moved to the `Widget.DashCast.ListItem.Leading` style (applied on the `ImageView`), where AppCompat resolves theme attributes reliably; `scaleType=fitCenter` was added so the 24dp glyphs render centered inside the 28dp slot. **Settings — À propos card rebuilt to match the mockup**: now contains four rows: (1) `Version` (`info` icon, dynamic `BuildConfig` value); (2) **`Rechercher une mise à jour`** (`system_update` icon + text-button `Vérifier` `btn_check_update`, opens `https://github.com/Kiroha/byd-dashcast/releases`); (3) **`Code source`** (`code` icon + trailing `open_in_new` chevron, row id `row_source_code`, full-row ripple via `?attr/selectableItemBackground`, opens `https://github.com/Kiroha/byd-dashcast`); (4) the pre-release `MaterialSwitch` `cb_prerelease` (icon switched from `system_update` to `science` to avoid duplicating the update-check glyph). Added three vector drawables: `ic_code`, `ic_open_in_new`, `ic_science`. Added a null- and intent-safe `openUrl()` helper in `SettingsActivity` (`Intent.ACTION_VIEW`, `FLAG_ACTIVITY_NEW_TASK`, Toast fallback). All existing widget IDs (`rg_cluster_type`, `rb_88/123/1025`, `sb_inset_h/v`, `tv_inset_h/v_value`, `cb_visual_mode`, `cb_boot_auto_start`, `cb_show_category_filters`, `cb_reconnect_popup`, `cb_prerelease`, `ll_sliders_mode`, `btn_apply_overscan`, `btn_reset_overscan`, `tv_overscan_result`, `btn_apply_top`, `tv_version_value`) preserved. No business logic was modified — only layout, styles, drawables, and three new click bindings. |
| **0.9.2-alpha** | 122 | **M3 Mockup-Accurate Redesign — Welcome + Settings**. **Welcome screen**: rebuilt from the mockup verbatim with a 96dp tonal hero square containing a new `cast_connected` vector glyph (`drawable/ic_cast.xml`), a large 44sp "DashCast" title, a primary-tinted dynamic version subtitle (`BYD CLUSTER MIRROR · v<versionName>`, populated from `BuildConfig.VERSION_NAME` in `WelcomeActivity.onCreate`), an `md_surface_container` card (28dp corners, 56/80dp padding, 0 elevation), and the 12 language buttons rendered as outlined pills (`Widget.DashCast.Button.Outlined`, 14dp gap). **Settings screen**: full mockup-fidelity rebuild as a true two-column layout. New top bar with title and a tonal `Appliquer` button (`btn_apply_top`, mirrors the in-card Apply action). Left column = cluster type (3 list-items: radio + headline + supporting text inside a single 20dp `MaterialCardView` on `md_surface_container`) + overscan margins (compact visual-mode switch row at the top, then per-axis Vertical / Horizontal sliders with leading `remove` / trailing `add` icons and a `colorPrimaryContainer` value badge, plus the existing visual D-pad mode, plus the Filled / Outlined Apply / Reset button pair and the monospace result panel). Right column = behaviour toggles (`MaterialSwitch` list-items with leading icons `restart_alt`, `autorenew`, `filter_list` — `cb_boot_auto_start`, `cb_reconnect_popup`, `cb_show_category_filters`) + about card (info icon + dynamic version label `0.9.2 · build 122`, system_update icon + pre-release `MaterialSwitch` `cb_prerelease`). New uppercase section headers in `colorPrimary` with 0.15 letter-spacing matching the mockup. **All existing widget IDs preserved** (`rg_cluster_type`, `rb_88/123/1025`, `sb_inset_h/v`, `tv_inset_h/v_value`, `cb_visual_mode`, `cb_boot_auto_start`, `cb_show_category_filters`, `cb_reconnect_popup`, `cb_prerelease`, `ll_sliders_mode`, `btn_apply_overscan`, `btn_reset_overscan`, `tv_overscan_result`). New IDs are purely cosmetic (`btn_apply_top`, `tv_version_value`). **SettingsActivity.java**: changed the `cb*` field type from `CheckBox` to `CompoundButton` so the layout can swap to `MaterialSwitch` without breaking the cast (both inherit `setChecked` / `isChecked` / `setOnCheckedChangeListener`). Added the top-bar `Apply` click binding and the dynamic version text. Added 8 new vector drawables (`ic_cast`, `ic_restart_alt`, `ic_autorenew`, `ic_filter_list`, `ic_info_outline`, `ic_remove`, `ic_add`, `ic_save`, `ic_system_update`) and a `bg_value_badge` shape. Added the reusable list-item styles in `styles_m3.xml` (`Widget.DashCast.SectionHead`, `Widget.DashCast.ListItem`, `Widget.DashCast.ListItem.Leading/Content/Headline/Support`, `Widget.DashCast.ValueBadge`). No business logic was modified — cluster type, overscan, boot-auto-start, reconnect popup, category filters, pre-release channel and visual mode all save and load exactly as before. |
| **0.9.1-alpha** | 120 | **Material 3 Migration — Phases 2 & 3 (Reusable Widgets + Welcome & Settings)**: Phase 2 adds a complete library of reusable Material 3 widget styles in `values/styles_m3.xml` (`Widget.DashCast.Button.Filled` / `.Tonal` / `.Outlined` / `.Text` / `.Error` / `.Small`, `Widget.DashCast.IconButton` and `.IconButton.Filled`, `Widget.DashCast.Fab` and `.Fab.Extended`, `Widget.DashCast.Card.Filled` / `.Outlined` / `.Elevated` with 16dp corners and 20dp content padding, `Widget.DashCast.Switch` / `.CheckBox` / `.RadioButton`, `Widget.DashCast.Chip.Filter` / `.Assist`, plus M3 text appearances `TextAppearance.DashCast.HeadlineSmall` / `TitleLarge` / `TitleMedium` / `BodyLarge` / `BodyMedium` / `SectionHeader`). No existing layout is touched in phase 2, so it is a zero-regression refactor. Phase 3 migrates the **Welcome screen** (`activity_welcome.xml`) and the **Settings screen** (`activity_settings.xml`) to Material 3: hard-coded dark backgrounds (`#0D0D0D`, `#1A237E`) and `@color/bg_main` / `@color/bg_card` are replaced by M3 attributes (`?attr/colorSurface`, `?attr/colorSurfaceVariant`, `?attr/colorOnSurface`, `?attr/colorOnSurfaceVariant`, `?attr/colorPrimary`), each section is now rendered inside a `MaterialCardView` with 16dp rounded corners and tonal surface, all 12 language buttons of the welcome screen use the new tonal style (`Widget.DashCast.Button.Tonal`), the apply/reset overscan buttons become a filled / tonal pair, all checkboxes and radio buttons are upgraded to `MaterialCheckBox` / `MaterialRadioButton` with M3 typography, and the overscan result panel uses the new `md_surface_container_highest` token. **Critically, every `android:id` is preserved** (`btn_lang_fr/en/de/it/tr/es/ru/uk/ar/uz/kk/be`, `rg_cluster_type`, `rb_88/123/1025`, `sb_inset_h/v`, `tv_inset_h/v_value`, `cb_visual_mode`, `cb_boot_auto_start`, `cb_show_category_filters`, `cb_reconnect_popup`, `cb_prerelease`, `ll_sliders_mode`, `btn_apply_overscan`, `btn_reset_overscan`, `tv_overscan_result`) so `WelcomeActivity.java` and `SettingsActivity.java` keep working without any code change. No business logic was modified, only XML structure and styling. |
| **0.9.0-alpha** | 119 | **Material 3 Migration — Phase 1 (Foundation)**: First step of a phased migration to Material Design 3 matching `mockups/mockup_m3.html`. Bumped `compileSdkVersion` from 29 to 33 (`targetSdkVersion` kept at 29, runtime behavior on the Seal is unchanged). Added the `com.google.android.material:material:1.9.0` dependency. Switched `AppTheme` parent from `Theme.AppCompat.DayNight.DarkActionBar` to `Theme.Material3.DayNight.NoActionBar`. Introduced a full Material 3 color token palette (`md_primary`, `md_on_primary`, `md_primary_container`, `md_surface_container_*`, `md_outline`, etc.) in both `values/colors.xml` (light scheme) and `values-night/colors.xml` (dark scheme matching the mockup, source color `#1565C0`). All legacy color names (`bg_main`, `text_primary`, `colorPrimary`, ...) and every existing layout are left untouched, so this phase introduces no functional or layout regression — only the underlying theme system is upgraded so that subsequent phases can migrate the screens one by one to Material 3 components and visuals. |
| **0.8.7-alpha** | 118 | **UX: Reconnect Popup Default Off + i18n**: The cluster reconnect popup is now disabled by default (opt-in via Settings) to avoid intrusive prompts for new users. Added `settings_reconnect_popup` translation string in all 12 supported locales (fr, en, de, es, it, ar, be, kk, ru, tr, uk, uz). Corrected French wording. |
| **0.8.6-alpha** | 117 | **Feature: Reconnect Popup Opt-In + FloatingRemoteButton Race Fix**: Made the "relaunch last app on cluster reconnect" popup fully opt-in via a new checkbox in Settings (`PREF_RECONNECT_POPUP`). Fixed a race condition in `FloatingRemoteButton` where `show()` was silently lost when called before the overlay was ready — added `sShouldBeVisible` static flag with deferred visibility after `createOverlay()` and `onStart()` re-sync. |
| **0.7.8-alpha** | 96 | **Boot/Recovery Safety**: Persist session cluster package tracking in SharedPreferences (`session_cluster_pkgs`) so state survives full car shutdown. Add boot-time cleanup of display affinity when auto-start projection is disabled (`boot_auto_start_enabled=false`). Add `MainActivity.onCreate` safety-net cleanup path. Keep cleanup disabled when auto-start projection is enabled to avoid user-facing regressions. |
| **0.7.7-alpha** | 95 | **Features + UX + Display Affinity**: Add category filters (All / Navigation / Media) with Settings toggle. Add floating quick-switch popup (recent cluster apps). Add profile launch mode (Driving/Parking app picker). Add per-app usage statistics dialog. Replace cluster-unavailable toast with activation dialog offering immediate projection start and deferred app launch. Move tracked apps back to Display 0 on projection stop and before red-cross kill to prevent apps relaunching on hidden VirtualDisplay. |
| **0.7.6-alpha** | 94 | **Theme & Clarity**: Fix DayNight follow-system behavior by setting default night mode at application startup. Improve Settings wording clarity for visual adjustment mode. |
| **0.7.5-alpha** | 93 | **Stability & Cleanup**: Fix shortcut crash (`LauncherApps.startShortcut` replaces broken `startActivity`). Fix BootReceiver SharedPrefs mismatch (auto-boot toggle was broken). Fix Settings locale not applied. Fix premature APK delete in UpdateChecker. Fix redirect depth limit in OTA. Fix TeleNav ghost-nav black screen. Fix FloatingRemoteButton handler leak. Full dark mode color pass (70+ hardcoded colors replaced). Remove dead code (`stopProjection`, orphan resources). |
| **0.7.4-alpha** | 92 | **Feature**: Native Android App Shortcuts support (list + grid view with popup menu). Smart app categorization (Navigation / Media / Others). Translated category labels (12 locales). |
| **0.7.3-alpha** | 91 | **Feature / UX**: Smart sorting logic for launched apps. Complete transition to Dynamic DayNight theme for comfortable night driving. Live, visually-scaled Overscan configuration mode. Auto-Boot receiver allows the app to automatically start the Cluster projection service when the car powers on. Dead code & UI diagnostics cleanup. New features translated to 12 languages. |
| **0.5.1** | 78 | **Release**: Comprehensive i18n support localized hardcoded strings across UI, overscan layouts, and system Toasts in all 12 supported languages. Fixed missing "Adjust" button from the manual UI diagram. |
| **0.4.3-alpha** | 73 | **Enhancement / Fix**: Removed Native Library extraction (\`extractNativeLibs=false\`) to drastically reduce install time and zero-out duplicate storage load. Corrected the URI Share Permission flags across all FileProvider Intents so Logs and Sniffers export successfully without crashing the OS. |
| **0.4.2-alpha** | 72 | **Critical BugFix**: Re-engineered ADB Task Identifier extractor using \`awk\` pattern matching to properly correlate BYD's multiline \`dumpsys\` outputs, ensuring the Red Cross accurately finds and removes orphaned Tasks in the Recents pool. |
| **0.4.1-alpha** | 71 | **Revert**: The red-cross kill button no longer drops the cluster projection. It only stops the app memory process, leaving the cluster display ready (black screen) for a new app cast without needing to restart the whole projection layer. |
| **0.4.0-alpha** | 70 | **Critical BugFix**: Resolved the red-cross force-stop on DiLink 3.0 via strict `app_process64` syntax. When killing an app currently running, DashCast will now automatically release the cluster and return back to the standard BYD Speedometer to prevent the black frozen screen illusion. |
| **0.3.3-alpha** | 69 | **Fix**: Corrected ADB kill app action (`TaskRemover` headless invocation syntax) and made mirror dynamic \`Apply\` button utilize \`wm overscan\` like the settings. |
| **0.3.2-alpha** | 68 | **Diag: ADAS Toggle** — Added two direct ADB buttons in the Diag tool to manually and easily send `sendInfo(1000, 47)` (ON) and `sendInfo(1000, 48)` (OFF) to toggle BYD's secret ADAS debugging mode on the cluster. |
| **0.3.1-alpha** | 67 | **Optimisation: Elegant App Killer** — Replaced the blind Binder sequence (service call 23/24) with a custom headless Java process (TaskRemover). DashCast now uses Android's native reflection inside an ADB shell to gracefully and accurately find the correct removeTask function regardless of the DiLink version or OEM tweaks. |
| **0.3.0-alpha** | 66 | **Feature: Per-App Resizing!** — You can now save individual Horizontal & Vertical inset values for each application separately using quick-access sliders directly accessible under the Mirror preview panel in DashCast, while the target app is running! (No more global resizing conflicts between Media and Navigation apps) |
| **0.2.95-alpha** | 65 | **Hotfix 5: Ghost Apps in Recents (Android 10/DiLink 3.0)** — Fixed the regex matching for ADB task removal. DashCast now successfully identifies and removes the active Task ID from the App Manager (Recents) so DiLink no longer mistakenly considers the app as running in the background after clicking the Red Cross. |
| **0.2.94-alpha** | 64 | **Hotfix 4: Kill Button Behavior** — Fixed an issue where terminating the active app with the red cross also stopped the ClusterService projection engine. The VirtualDisplay is now left intact when killing an app, allowing the user to seamlessly launch another app to the dashboard without needing to re-activate the engine. Note: Original fix failed injection sync. |
| **0.2.93-alpha** | 63 | **Hotfix 3: Kill Button Behavior** — Fixed an issue where terminating the active app with the red cross also stopped the ClusterService projection engine. The VirtualDisplay is now left intact when killing an app, allowing the user to seamlessly launch another app to the dashboard without needing to re-activate the engine. |
| **0.2.92-alpha** | 62 | **Hotfix 2: Android 10 am task compatibility** — Further fallbacks added to delete the ghost tasks in Android UI. Attempts `am task rm`, `am stack remove` or a low-level Binder call `service call activity 23` depending on ROM customizations. Warnings suppressed to allow the final `am force-stop` to execute cleanly. |
| **0.2.91-alpha** | 61 | **Hotfix: Android 10 am task compatibility** — Replaced `am task rm` with the formally valid `am task remove` command to ensure the Recents UI ghostly task is successfully deleted before `am force-stop`. |
| **0.2.9-alpha** | 60 | **Process & Task Killer** — Fixed an issue where terminating an app with the red cross only killed its background processes but left it in the 'Recent Apps' overview. ADB now identifies `TaskRecord` IDs and executes `am task rm` to fully clear them from Recents UI before issuing `force-stop`. |
| **0.2.8-alpha** | 59 | **Interactive OTA UI** — Enhanced the Over-The-Air updater. Users are now presented with a dialog displaying the full changelog natively fetched from GitHub releases. Added explicit 'Update Now' vs 'Update Later' choices rather than forced auto-download. |
| **0.2.7-alpha** | 58 | **Auto-Launch Checkbox Fix** — Restored visibility and binding logic for the 'Auto' checkbox in the list/grid views. |
| **0.2.6-alpha** | 57 | **Floating Button Fix** — Fixed Smart Overlay Button spawn bug (immediate transparency and off-screen misplacement). Elevated opacity and timing logic to restore correct visibility sync. |
| **0.2.5-alpha** | 56 | **Dark Text Fix** — Grid mode app labels corrected to Dark Gray for visibility against light backgrounds. Duplicate checkbox fixed. |
| **0.2.4-alpha** | 55 | **Auto-Launch & Grid View** — Implemented 5-column Grid layout. Added Boot Auto-Launch exclusivity (Radio behavior) and "Favorites" persistence via Long-Press. |
| **0.2.3-alpha** | 54 | **QoL UI Update** — Removed Split-screen logic. Added Top Overflow menu with List/Grid toggle switch. Persistent state management via SharedPreferences. |

## Public releases

| Version | versionCode | Summary |
|---------|-------------|---------|
| **0.5.1** | 78 | **Release**: Comprehensive i18n support localized hardcoded strings across UI, overscan layouts, and system Toasts in all 12 supported languages. Fixed missing "Adjust" button from the manual UI diagram. |
| **0.4.3-alpha** | 73 | **Enhancement / Fix**: Removed Native Library extraction (\`extractNativeLibs=false\`) to drastically reduce install time and zero-out duplicate storage load. Corrected the URI Share Permission flags across all FileProvider Intents so Logs and Sniffers export successfully without crashing the OS. |
| **0.4.2-alpha** | 72 | **Critical BugFix**: Re-engineered ADB Task Identifier extractor using \`awk\` pattern matching to properly correlate BYD's multiline \`dumpsys\` outputs, ensuring the Red Cross accurately finds and removes orphaned Tasks in the Recents pool. |
| **0.4.1-alpha** | 71 | **Revert**: The red-cross kill button no longer drops the cluster projection. It only stops the app memory process, leaving the cluster display ready (black screen) for a new app cast without needing to restart the whole projection layer. |
| **0.4.0-alpha** | 70 | **Critical BugFix**: Resolved the red-cross force-stop on DiLink 3.0 via strict `app_process64` syntax. When killing an app currently running, DashCast will now automatically release the cluster and return back to the standard BYD Speedometer to prevent the black frozen screen illusion. |
| **0.3.3-alpha** | 69 | **Fix**: Corrected ADB kill app action (`TaskRemover` headless invocation syntax) and made mirror dynamic \`Apply\` button utilize \`wm overscan\` like the settings. |
| **0.3.2-alpha** | 68 | **Diag: ADAS Toggle** — Added two direct ADB buttons in the Diag tool to manually and easily send `sendInfo(1000, 47)` (ON) and `sendInfo(1000, 48)` (OFF) to toggle BYD's secret ADAS debugging mode on the cluster. |
| **0.3.1-alpha** | 67 | **Optimisation: Elegant App Killer** — Replaced the blind Binder sequence (service call 23/24) with a custom headless Java process (TaskRemover). DashCast now uses Android's native reflection inside an ADB shell to gracefully and accurately find the correct removeTask function regardless of the DiLink version or OEM tweaks. |
| **0.3.0-alpha** | 66 | **Feature: Per-App Resizing!** — You can now save individual Horizontal & Vertical inset values for each application separately using quick-access sliders directly accessible under the Mirror preview panel in DashCast, while the target app is running! (No more global resizing conflicts between Media and Navigation apps) |
| **0.2.95-alpha** | 65 | **Hotfix 5: Ghost Apps in Recents (Android 10/DiLink 3.0)** — Fixed the regex matching for ADB task removal. DashCast now successfully identifies and removes the active Task ID from the App Manager (Recents) so DiLink no longer mistakenly considers the app as running in the background after clicking the Red Cross. |
| **0.2.94-alpha** | 64 | **Hotfix 4: Kill Button Behavior** — Fixed an issue where terminating the active app with the red cross also stopped the ClusterService projection engine. The VirtualDisplay is now left intact when killing an app, allowing the user to seamlessly launch another app to the dashboard without needing to re-activate the engine. Note: Original fix failed injection sync. |
| **0.2.93-alpha** | 63 | **Hotfix 3: Kill Button Behavior** — Fixed an issue where terminating the active app with the red cross also stopped the ClusterService projection engine. The VirtualDisplay is now left intact when killing an app, allowing the user to seamlessly launch another app to the dashboard without needing to re-activate the engine. |
| **0.2.92-alpha** | 62 | **Hotfix 2: Android 10 am task compatibility** — Further fallbacks added to delete the ghost tasks in Android UI. Attempts `am task rm`, `am stack remove` or a low-level Binder call `service call activity 23` depending on ROM customizations. Warnings suppressed to allow the final `am force-stop` to execute cleanly. |
| **0.2.91-alpha** | 61 | **Hotfix: Android 10 am task compatibility** — Replaced `am task rm` with the formally valid `am task remove` command to ensure the Recents UI ghostly task is successfully deleted before `am force-stop`. |
| **0.2.9-alpha** | 60 | **Process & Task Killer** — Fixed an issue where terminating an app with the red cross only killed its background processes but left it in the 'Recent Apps' overview. ADB now identifies `TaskRecord` IDs and executes `am task rm` to fully clear them from Recents UI before issuing `force-stop`. |
| **0.2.8-alpha** | 59 | **Interactive OTA UI** — Enhanced the Over-The-Air updater. Users are now presented with a dialog displaying the full changelog natively fetched from GitHub releases. Added explicit 'Update Now' vs 'Update Later' choices rather than forced auto-download. |
| **0.2.7-alpha** | 58 | **Auto-Launch Checkbox Fix** — Restored visibility and binding logic for the 'Auto' checkbox in the list/grid views. |
| **0.2.1-beta** | 52 | **Bugfixes & Optimizations** — Resources leak fix (`try-with-resources` on MirrorDaemon), `UnspecifiedImmutableFlag` intent fix on UpdateChecker, Manifest `android:exported=true` compliance, `StaticFieldLeak` suppression. Huge storage cleanup removing old 800MB orphaned `PackageInstaller` sessions. App strings purged. |
| **0.2.0-beta** | 51 | **OTA Update System** — Addition of the self-updating OTA mechanism (Silent update or Prompts). Transitioned project to Beta. |
| **0.1.49-alpha** | 49 | **Consolidation (Breaking Change)** — Package ID renamed. Heavy README update (badges, ota, compatibility). Diagnostics log crashes fixed. Refined Watchdog reliability. End of the Alpha cycle. |
| **0.1.39-alpha** | 39 | **Slavic/Central Asian i18n & Core Fixes** — Added ES, RU, UK, AR, BE, KK, UZ languages. Enhanced `restoreOriginCluster` safety with delays. Fixed PID resolution via `/proc` directly. |
| **0.1.31-alpha** | 32 | **DiLink5 resize test (Section 7)** — Isolated IATM test in DiagActivity: WM:4→5 + `resizeTask(RESIZE_MODE_FORCED=3)` via raw Binder on `activity_task`. Sanity: lint fixes (WrongConstant, DefaultLocale, MissingTranslation ×4 locales, RedundantLabel). |
| **0.1.30-alpha** | 31 | **Strings + UI cleanup** — Remove cluster keyboard button. Remove ADAS diagnostic buttons (cmd 12/13 confirmed without effect on 2D cluster Seal EU). String cleanup ×5 locales. |
| **0.1.29-alpha** | 30 | **Settings: overscan tuning** — New SettingsActivity screen: H/V inset tuning with immediate apply. Rename main screen buttons for clarity (Activate / Stop / Restore Original). |
| **0.1.28-alpha** | 29 | **ADAS diagnostic buttons** — Diag section 3: ADAS ON/OFF buttons (cmd 12/13) for 3D cluster testing. |
| **0.1.27-alpha** | 28 | **Bounds via `wm overscan`** — Replace dead IATM bounds approach with `wm overscan -d <displayId>`. Safety guard: restrict overscan to cluster display only + reset button for display 0. |
| **0.1.26-alpha** | 27 | **State persistence + storage cleanup** — Persist active cluster app to survive Activity recreation (SharedPreferences). Diag section 6: storage cleanup (logs, reports, sniffer files, log buffer). FREEFORM bounds: retry via IATM after launch. |
| **0.1.25-alpha** | 26 | **Logs menu + i18n refactor** — Overflow menu: new "📜 Logs" entry → LogActivity. Remove 69 dead strings. Migrate DiagActivity to fully localized resources. |
| **0.1.24-alpha** | 25 | **Sanity #1+#2** — Dead code and dead fields removed. `ClusterMirrorManager` blank line fix. DiagActivity: remove obsolete Freedom tests, unify comment numbering [1]-[5]. |
| **0.1.23-alpha** | 24 | **`moveTaskToDisplay` + kill detection** — Move tasks between displays without force-stop + relaunch. `OnUidImportanceListener` detects external app kill and clears stale cluster state. |
| **0.1.22-alpha** | 23 | **Mirror shortcut + overlay refactor** — Replace GPS floating button with 📺 mirror shortcut. New 📺 status bar button (visible when an app is on the cluster). Remove floating log overlay button. |
| **0.1.21-alpha** | 22 | **Security audit + Telegram share** — Full display-operation audit: guard `wm overscan`, `am display`, ADB commands to cluster display only. Long-press on any result TextView → share to Telegram. |
| **0.1.20-alpha** | 21 | **RE Sniffer fix + FREEFORM bounds** — RE Sniffer: `setsid` prefix, header sync, logcat without process filter. FREEFORM inset bounds: prevent app clipping at cluster screen edges. Fix: stop unintended `AppStartManagement` in fallback activation path. |
| **0.1.19-alpha** | 20 | **AutoDisplayService control + sanity** — Binder control of AutoDisplayService. Resource leak fixes. Sanity checks #5→#8 complete. README updated. |
| **0.1.18-alpha** | 19 | **DiagActivity refactor + DashCastDaemon** — Remove Freedom diagnostic tests. Add Headless System RE Sniffer (deep dumpsys + broadcast tracking). Add DashCastDaemon (`app_process` VirtualDisplay probe) and TEST 14-16. |
| **0.1.17-alpha** | 18 | **Complete Freedom removal + sanity** — Remove `checkAndStartWithFreedom()` and all `FreedomStatus` code from ClusterService/MainActivity. BYD native cluster spawn via `AppStartManagement`. Sanity #5-#7: empty catches logged, dead code, JMM fixes. |
| **0.1.16-alpha** | 17 | **Sanity + test build 04/05/2026** — Dead code removal, duplicate `setEnabled` fix, DiagActivity comment cleanup. Internal test build bump versionCode 17. |
| **0.1.15-alpha** | 16 | **Overlay cleanup + DashCastDaemon fix** — Remove floating log overlay button. Fix DashCastDaemon: bypass packageName UID mismatch preventing `app_process` daemon start. |
| **0.1.14-alpha** | 15 | **Display 0 security** — Full audit: all display-targeted operations (`wm overscan`, ADB shell commands) restricted to cluster display. Prevent accidental main screen modification. |
| **0.1.13-alpha** | 14 | **Mirror shortcut in status bar** — 📺 button appears in the cluster control panel when an app is active on the cluster. Long-press result TextViews → share to Telegram. |
| **0.1.12-alpha** | 13 | **FREEFORM inset bounds + RE Sniffer** — Inset margins applied via `setLaunchBounds` to prevent app clipping on cluster edges. RE Sniffer: `setsid`, header sync, raw logcat mode. |
| **0.1.11-alpha** | 12 | **AutoDisplayService + sanity #8** — Binder control of `AutoDisplayService` added. Resource leaks fixed. All sanity checks #1→#8 complete (versionCode bump). |
| **0.1.10-alpha** | 11 | **DiagActivity refactor** — Remove Freedom-dependent tests. Add Headless System Sniffer (logcat, `am monitor`, `dumpsys` via ADB). Sanity check #7: orphan imports, dead strings. |
| **0.1.9-alpha** | 10 | **Freedom removal** — Complete removal of Freedom (com.xdja.clusterdemo) from codebase. BYD native cluster spawn via `com.byd.appstartmanagement`. Sanity #6: JMM correctness, dead code cleanup. |
| **0.1.8-alpha** | 9 | **Sanity checks #1–#5** — Empty catches now logged (no more silent failures), dead methods removed, orphan imports cleaned. |
| **0.1.7-alpha** | 8 | **Freedom-free** — VirtualDisplay cluster creation confirmed via live logcat RE (sendInfo 30→16→35). Full autonomous activation sequence. Sanity fixes (duplicate setEnabled, color reset). README cleaned. |
| **0.1.6-alpha** | 7 | DiLink 5 Dashboard RE — deep dumpsys sniffer, broadcast tracking, DiagActivity refactor |
| **0.1.5-alpha** | — | Stability improvements — last release with Freedom dependency |
| **0.1.4-alpha** | — | Fix touch offset bug in cluster mirror input forwarding |
| **0.1.3-alpha** | — | Full i18n (FR/EN/DE/IT/TR), string externalization |
| **0.1.2-alpha** | — | EN translation, DashCast rename, new icon — device-validated |
| **0.1.1-alpha** | — | Bug fixes, code sanity, README improvements |
| **0.1.0-alpha** | — | First public release — cluster mirror working (image + touch) |

---

## Internal versions (pre-public)

| Version | versionCode | Summary |
|---------|-------------|---------|
| **0.5.1** | 78 | **Release**: Comprehensive i18n support localized hardcoded strings across UI, overscan layouts, and system Toasts in all 12 supported languages. Fixed missing "Adjust" button from the manual UI diagram. |
| **0.4.3-alpha** | 73 | **Enhancement / Fix**: Removed Native Library extraction (\`extractNativeLibs=false\`) to drastically reduce install time and zero-out duplicate storage load. Corrected the URI Share Permission flags across all FileProvider Intents so Logs and Sniffers export successfully without crashing the OS. |
| **0.4.2-alpha** | 72 | **Critical BugFix**: Re-engineered ADB Task Identifier extractor using \`awk\` pattern matching to properly correlate BYD's multiline \`dumpsys\` outputs, ensuring the Red Cross accurately finds and removes orphaned Tasks in the Recents pool. |
| **0.4.1-alpha** | 71 | **Revert**: The red-cross kill button no longer drops the cluster projection. It only stops the app memory process, leaving the cluster display ready (black screen) for a new app cast without needing to restart the whole projection layer. |
| **0.4.0-alpha** | 70 | **Critical BugFix**: Resolved the red-cross force-stop on DiLink 3.0 via strict `app_process64` syntax. When killing an app currently running, DashCast will now automatically release the cluster and return back to the standard BYD Speedometer to prevent the black frozen screen illusion. |
| **0.3.3-alpha** | 69 | **Fix**: Corrected ADB kill app action (`TaskRemover` headless invocation syntax) and made mirror dynamic \`Apply\` button utilize \`wm overscan\` like the settings. |
| **0.3.2-alpha** | 68 | **Diag: ADAS Toggle** — Added two direct ADB buttons in the Diag tool to manually and easily send `sendInfo(1000, 47)` (ON) and `sendInfo(1000, 48)` (OFF) to toggle BYD's secret ADAS debugging mode on the cluster. |
| **0.3.1-alpha** | 67 | **Optimisation: Elegant App Killer** — Replaced the blind Binder sequence (service call 23/24) with a custom headless Java process (TaskRemover). DashCast now uses Android's native reflection inside an ADB shell to gracefully and accurately find the correct removeTask function regardless of the DiLink version or OEM tweaks. |
| **0.3.0-alpha** | 66 | **Feature: Per-App Resizing!** — You can now save individual Horizontal & Vertical inset values for each application separately using quick-access sliders directly accessible under the Mirror preview panel in DashCast, while the target app is running! (No more global resizing conflicts between Media and Navigation apps) |
| **0.2.95-alpha** | 65 | **Hotfix 5: Ghost Apps in Recents (Android 10/DiLink 3.0)** — Fixed the regex matching for ADB task removal. DashCast now successfully identifies and removes the active Task ID from the App Manager (Recents) so DiLink no longer mistakenly considers the app as running in the background after clicking the Red Cross. |
| **0.2.94-alpha** | 64 | **Hotfix 4: Kill Button Behavior** — Fixed an issue where terminating the active app with the red cross also stopped the ClusterService projection engine. The VirtualDisplay is now left intact when killing an app, allowing the user to seamlessly launch another app to the dashboard without needing to re-activate the engine. Note: Original fix failed injection sync. |
| **0.2.93-alpha** | 63 | **Hotfix 3: Kill Button Behavior** — Fixed an issue where terminating the active app with the red cross also stopped the ClusterService projection engine. The VirtualDisplay is now left intact when killing an app, allowing the user to seamlessly launch another app to the dashboard without needing to re-activate the engine. |
| **0.2.92-alpha** | 62 | **Hotfix 2: Android 10 am task compatibility** — Further fallbacks added to delete the ghost tasks in Android UI. Attempts `am task rm`, `am stack remove` or a low-level Binder call `service call activity 23` depending on ROM customizations. Warnings suppressed to allow the final `am force-stop` to execute cleanly. |
| **0.2.91-alpha** | 61 | **Hotfix: Android 10 am task compatibility** — Replaced `am task rm` with the formally valid `am task remove` command to ensure the Recents UI ghostly task is successfully deleted before `am force-stop`. |
| **0.2.9-alpha** | 60 | **Process & Task Killer** — Fixed an issue where terminating an app with the red cross only killed its background processes but left it in the 'Recent Apps' overview. ADB now identifies `TaskRecord` IDs and executes `am task rm` to fully clear them from Recents UI before issuing `force-stop`. |
| **0.2.8-alpha** | 59 | **Interactive OTA UI** — Enhanced the Over-The-Air updater. Users are now presented with a dialog displaying the full changelog natively fetched from GitHub releases. Added explicit 'Update Now' vs 'Update Later' choices rather than forced auto-download. |
| **0.2.7-alpha** | 58 | **Auto-Launch Checkbox Fix** — Restored visibility and binding logic for the 'Auto' checkbox in the list/grid views. |
| 2.52 | 155 | **Mirror fully working** — TextureView (SurfaceFlinger as producer), `hardwareAccelerated=true` in manifest |
| 2.51 | 154 | SurfaceView → TextureView migration (`new Surface(SurfaceTexture)`) |
| 2.17 | 122 | Bugfix: Attach click listeners to sniffer & daemon buttons in DiagActivity |
| 2.16 | 121 | Added Headless System Sniffer (logcat, am monitor, dumpsys via ADB) with Export button |
| 2.15 | 120 | Fix native SurfaceControl mirror restoration |
| 2.14 | 119 | Freedom proxy injected: `app_process` pseudo-daemon + diagnostics IPC testing |
| 2.09 | 114 | Perf & sanity: `AppLogger` O(1) ArrayDeque, `ExecutorService` in `AdbLocalClient` |
| 2.08 | 113 | Fix double `startFreedom()` race condition, `freedomJustStarted` flag |
| 2.07 | 112 | Sanity fixes: `AsyncTask`→`Executors`, adapter O(1) HashMap index |
| 2.06 | 111 | Freedom state check at startup (NOT_INSTALLED / INACTIVE / ACTIVE) |
| 2.05 | 110 | Mirror screencap fallback, persist `PREF_MAIN_PKG`, split bounds via extras |
| 2.04 | 109 | Dead code removal + `resolveLayerStack()` fix (`displayId<<16`) |
| 2.03 | 108 | `unlockHiddenApis()` VMRuntime + `createDisplay` fallback `secure=true` |
| 2.02 | 107 | Fix Freedom (activity name + fission check), split relaunch bounds |
| 2.01 | 106 | `startFreedom()`: write `navigationType=1` via ObjectOutputStream |
| 2.00 | 105 | Freedom headless (auto foreground return), ClusterService foreground refactor |
| 1.94 | 99  | Cluster split 50/50 (`launchTrampolineWithBounds`) |
| 1.91 | 96  | Real-time SurfaceControl mirror (replaces bitmap screenshot), Freedom reset 全屏 |
| 1.73 | 74  | Exported trampoline + ADB uid=2000 launch (bypasses INTERNAL_SYSTEM_WINDOW) |
| 1.46 | 47  | cmd30 before cmd16 sequence — fixes ADAS stretching |
