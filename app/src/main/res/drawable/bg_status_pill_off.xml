<?xml version="1.0" encoding="utf-8"?>
<!-- Service off. Neutral, not alarming: a stopped service is a state, not a fault. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="999dp"/>
    <solid android:color="@color/md_pill_off_fill"/>
    <stroke android:width="1dp" android:color="@color/md_pill_off_stroke"/>
</shape>
