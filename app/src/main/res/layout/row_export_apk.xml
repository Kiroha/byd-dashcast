<?xml version="1.0" encoding="utf-8"?>
<!--
    Export APK row — log-style line + Export button on the right.
    Mirrors the visual language of row_log.xml so users immediately recognise
    the « one line per item » convention from the Journal screen.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="HardcodedText"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:layout_marginBottom="6dp"
    android:paddingStart="0dp" android:paddingEnd="14dp"
    android:paddingTop="10dp" android:paddingBottom="10dp"
    android:background="@drawable/bg_log_row_info">

    <!-- Color bar (always primary tint — these are inventory rows, not test results) -->
    <View
        android:layout_width="4dp"
        android:layout_height="match_parent"
        android:minHeight="40dp"
        android:layout_marginEnd="14dp"
        android:background="@color/md_primary"/>

    <!-- Pkg + version + path (monospace, multi-line) -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tv_export_apk_pkg"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="13sp"
                android:textStyle="bold"
                android:textColor="@color/md_primary"
                android:fontFamily="monospace"
                android:text="com.byd.example"/>

            <TextView
                android:id="@+id/tv_export_apk_version"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:textSize="12sp"
                android:textColor="@color/md_on_surface_variant"
                android:fontFamily="monospace"
                android:text="v?.? (0)"/>

            <View
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1"/>

            <TextView
                android:id="@+id/tv_export_apk_size"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="11sp"
                android:textColor="@color/md_on_surface_variant"
                android:fontFamily="monospace"
                android:text="-"/>
        </LinearLayout>

        <TextView
            android:id="@+id/tv_export_apk_path"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textSize="11sp"
            android:textColor="@color/md_on_surface_variant"
            android:fontFamily="monospace"
            android:singleLine="true"
            android:ellipsize="middle"
            android:text="/data/app/.../base.apk"/>
    </LinearLayout>

    <!-- Export button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_export_apk_row"
        style="@style/Widget.Material3.Button.TonalButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:minWidth="0dp"
        android:text="@string/diag_export_apk_btn_export"/>
</LinearLayout>
