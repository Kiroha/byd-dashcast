<?xml version="1.0" encoding="utf-8"?>
<!-- Layout preset card for LayoutManagerActivity RecyclerView -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="11dp"
    android:layout_marginStart="8dp"
    android:layout_marginEnd="8dp"
    android:layout_marginBottom="6dp"
    android:background="@drawable/bg_card_default"
    android:clickable="true"
    android:focusable="true">

    <!-- Header row: icon + name + active badge -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="6dp">

        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:padding="4dp"
            android:layout_marginEnd="8dp"
            android:scaleType="fitCenter"
            android:tint="@color/md_on_surface_variant" tools:ignore="UseAppTint"
            android:src="@drawable/ic_dashboard"
            android:contentDescription="@null"/>

        <TextView
            android:id="@+id/lm_card_name"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/md_on_surface"
            android:maxLines="1"
            android:ellipsize="end"/>

        <TextView
            android:id="@+id/lm_card_active_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/layout_preset_active_badge"
            android:textSize="10sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:background="#3A7D44"
            android:paddingStart="7dp"
            android:paddingEnd="7dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:visibility="gone"/>

    </LinearLayout>

    <!-- Meta row: slot count -->
    <TextView
        android:id="@+id/lm_card_meta"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:textColor="@color/md_on_surface_variant"
        android:layout_marginBottom="8dp"/>

    <!-- Action buttons row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="start">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/lm_card_btn_activate"
            style="@style/Widget.Material3.Button"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:text="@string/layout_preset_activate"
            android:textSize="11sp"
            android:minWidth="0dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:layout_marginEnd="6dp"
            app:cornerRadius="16dp"
            xmlns:app="http://schemas.android.com/apk/res-auto"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/lm_card_btn_edit"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:text="@string/fission_editor_edit"
            android:textSize="11sp"
            android:minWidth="0dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:layout_marginEnd="6dp"
            app:cornerRadius="16dp"
            xmlns:app="http://schemas.android.com/apk/res-auto"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/lm_card_btn_delete"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:text="@string/fission_editor_delete"
            android:textSize="11sp"
            android:minWidth="0dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:backgroundTint="@color/md_error_container"
            android:textColor="@color/md_on_error_container"
            app:cornerRadius="16dp"
            xmlns:app="http://schemas.android.com/apk/res-auto"/>

    </LinearLayout>

</LinearLayout>
